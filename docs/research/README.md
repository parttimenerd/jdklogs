# JFR Proposed Events — Research Index

Research documents supporting the proposed JFR events in `data/jfr-proposed-events.json`.

## Documents

### [Shenandoah Tenuring Threshold](shenandoah-tenuring-threshold.md)
How Shenandoah generational mode dynamically computes the tenuring threshold using mortality rate analysis. Covers the age census mechanism, the `compute_tenuring_threshold()` algorithm, `ShenandoahGenerationalCensusIgnoreOlderCohorts`, dark matter handling, and why this differs from G1/Parallel's static `MaxTenuringThreshold`. Supports `jdk.ShenandoahTenuringThreshold`.

### [G1 Concurrent Refinement](g1-concurrent-refinement.md)
How G1's dirty-card queue and concurrent refinement threads work. Covers the two distinct log sites (`print_refinement_stats` vs `adjust_threads_wanted`), card categories, the JDK 25 write barrier redesign (JEP 522), and why `jdk.G1ConcurrentRefinementSweep` + `jdk.G1ConcurrentRefinementPolicy` are needed.

### [ZGC Director Rules](zgc-director-rules.md)
How ZGC decides when to trigger Young and Old collections. Covers all rule types (Timer, AllocationRate dynamic/static, HighUsage, Warmup, Proactive), the early-exit evaluation model, the Major AllocationRate escalation path, and why the `jdk.ZDirectorRule` event is needed despite experimental `ZStatisticsCounter/Sampler` events.

### [Shenandoah Generational GC](shenandoah-generational.md)
How Shenandoah's generational mode (production-ready JDK 25, JEP 521) works. Covers collection types (young/old/global/mixed/degenerated/full), the regulator thread, remembered-set card statistics, MMU tracking, reclaim progress early-exit semantics, and the collection decision FSM. Supports `jdk.ShenandoahCardStatistics`, `jdk.ShenandoahMMU`, `jdk.ShenandoahReclaimProgress`, `jdk.ShenandoahCollectionDecision`.

### [Parallel GC Adaptive Sizing](parallel-gc-adaptive-sizing.md)
How `PSAdaptiveSizePolicy` implements the feedback loop for eden/survivor/old-gen sizing. Covers the throughput vs pause-time goal trade-off, gc-distance semantics, promoted bytes (averaged vs last), old-gen shrink, survivor overflow, and GCOverheadLimitExceeded in Parallel GC. Supports `jdk.PSAdaptiveSizePolicy` and `jdk.GCOverheadLimitExceeded`.

### [G1 Collection Set Candidates](g1-collection-set-candidates.md)
How G1 selects old-gen regions for mixed GC pauses. Covers the two separate selection paths (`select_candidates_from_marking()` and `select_candidates_from_retained()`), the groups concept, stop reasons, the optional evacuation threshold, and mixed-GC continuation decisions. Supports `jdk.G1CollectionSetCandidates`.

### [G1 Heap Resize Policy](g1-heap-resize.md)
How G1 resizes the committed heap between GC pauses using a CPU-usage deviation counter. Covers the `_gc_cpu_usage_deviation_counter` mechanism, upper/lower threshold derivation from `GCTimeRatio`, the shrink-path-only fields (`scaleFactorPct`, `freeRegions`, `regionsNeededForAlloc`), `atLimit` semantics, and the distinction from full-collection resizing. Supports `jdk.G1HeapResize`.

### [Native Heap Trimming](native-heap-trim.md)
Why native heap trimming matters for containerised JVMs (RSS bloat from glibc malloc arenas). Covers the `NativeHeapTrimmer` thread, trim operation mechanics, upstream PR history (JDK-8365306 closed), relationship to `jdk.ResidentSetSize`, and corrected `deltaBytes` sign semantics. Supports `jdk.NativeHeapTrim`.

### [ZGC Generational Mode](zgc-generational.md)
How ZGC selects a tenuring threshold each young collection. Covers the three selection paths (Promote All / ZTenuringThreshold config / Computed), the `compute_tenuring_threshold()` algorithm (life decay factor × log residency, scaled by allocation pressure), why `jdk.ZUncommit` already exists (EventZUncommit::commit() fires in update_statistics()), and why only the info-level result fields belong in the proposed event. Supports `jdk.ZGCTenuringThreshold`.

### [Event Consolidation & Emission-Point Analysis](event-consolidation.md)
Where each proposed event fires (exact function, call chain, cadence, thread), analysis of which events to merge/split, and field-level trimming recommendations. Key findings: `G1ConcurrentRefinementSweep`+`Policy` stay separate (different cadences/threads); `ShenandoahCollectionDecision`+`ReclaimProgress` stay separate (start vs end of cycle); `ZDirectorRule` should be redesigned as a per-tick summary (22 mostly-null fields → 6-field event); `ZNMethodRegistration.tableRebuilt` dropped (no source); `PSAdaptiveSizePolicy` loses 3 fields; `G1CollectionSetCandidates.continueMixed` dropped (different call site). `ShenandoahMMU.report()` path confirmed `log_debug`. Full emission-point table with calling function, cadence, and thread for all 16 events.


How Shenandoah generational mode decides when to start young and old collections. Covers the `log_trigger()` macro (resolves to `log_info(gc)` in production), the three adaptive heuristic trigger types (rate_average, rate_momentary, rate_accelerated), the three old heuristic triggers (expansion_failure, fragmentation, growth), the regulator FSM, and the `_margin_of_error_sd` field. Supports `jdk.ShenandoahCollectionDecision` trigger augmentation (critique pass 6).

- **Critique pass 57** (2026-08-18): Cross-event correlation pass — all 14 "What it is used for" sections now include explicit **Cross-event correlation** paragraphs naming the join key (gcId or startTime window), the correlated event(s), and the actionable insight from the join. (1) NativeHeapTrim: join by `startTime` window with `jdk.GarbageCollection` to detect GC-adjacent trim patterns; join with `jdk.ResidentSetSize` by time proximity to track net RSS growth vs. trim reclaim. (2) GCOverheadLimitExceeded: join on `gcId` with `jdk.GarbageCollection` (last GC type and duration before OOM), `jdk.G1HeapSummary`/`jdk.PSHeapSummary` (heap state after last GC). (3) ShenandoahMMU: join on `gcId` with `jdk.GarbageCollection` (STW fraction vs. GCU%) and `jdk.ProcessCPULoad` (GC fraction of total JVM CPU). (4) ShenandoahCollectionDecision: existing cross-refs verified complete (`jdk.ShenandoahReclaimProgress` badProgressCount chain). (5) ShenandoahReclaimProgress: join on `gcId` with `jdk.ShenandoahCollectionDecision` (trigger type determines escalation context) and `jdk.ShenandoahEvacuationInformation` (regionsEvacuated correlation); `badProgressCount` time-series from 0→1→2 pattern. (6) ShenandoahTenuringThreshold: join on `gcId` with `jdk.ShenandoahPromotionInformation` (threshold vs. actual promotion bytes), `jdk.TenuringDistribution` comparison context, and `jdk.ShenandoahCollectionDecision` `triggerType=growth` co-occurrence. (7) ZGCTenuringThreshold: join on `gcId` with `jdk.ZYoungGarbageCollection` (threshold validation), `jdk.ZOldGarbageCollection` (time-until-promote-all-triggers-old), `jdk.ZAllocationStall` (emergency path timing). (8) ZNMethodRegistration: join on `gcId` with `jdk.ZYoungGarbageCollection`/`jdk.ZOldGarbageCollection` (nmethod count vs. duration), `jdk.Deoptimization` (stale slot source), `jdk.CodeCacheStatistics` (table size vs. cache size). (9) G1ConcurrentRefinementSweep: join on `gcId` with `jdk.GarbageCollection` (backlog-to-pause-extension quantification) and by time window with `jdk.G1ConcurrentRefinementPolicy` (observed throughput vs. policy decision). (10) G1ConcurrentRefinementPolicy: join by time window with sweep events (threadsWanted vs. cardsPending trend) and `jdk.GarbageCollection` (predictedPendingCards vs. actual duration). (11) G1CollectionSetCandidates: join on `gcId` with `jdk.GarbageCollection` (selectedRegions vs. duration), `jdk.EvacuationInformation` (pinnedInQueue→overBudgetRegions), `jdk.G1AdaptiveIHOP` (IHOP pressure + marking throughput). (12) G1HeapResize: join on `gcId` with `jdk.G1HeapSummary` (resize intent vs. committed outcome), `jdk.GarbageCollection` (expand=true → duration trend), deviationCounter-at-threshold pattern for perpetual-undersizing diagnosis. (13) ZDirectorRule: join by time window with `jdk.ZYoungGarbageCollection`/`jdk.ZOldGarbageCollection` (triggered-rule-to-GC delay), `jdk.ZAllocationStall` (director accuracy validation). (14) PSAdaptiveSizePolicy: join on `gcId` with `jdk.GarbageCollection` (policy pause estimate vs. actual), `jdk.PSHeapSummary` (desiredEden vs. actual eden), `jdk.GCOverheadLimitExceeded` (shrink-then-OOM causal chain); oscillation detection via 3+ alternating edenSizingBranch events.

- **Critique pass 58** (2026-08-18): Second-tier precision pass (continued) — thin tuning-use entries upgraded. (1) G1HeapResize `lowerThresholdPct`: upgraded from bare formula to full diagnosis path — GCTimeRatio=24 worked example (target=4%, lower=3%), GCTimeRatio and G1CPUUsageDeviationPercent tuning actions, `atLimit=true` means `-Xms` is blocking shrink. (2) G1HeapResize `upperThresholdPct`: upgraded similarly — same worked example (upper=5%), expansion path after 4+ events with `deviationCounter ≥ G1CPUUsageExpandThreshold`, `atLimit=true` means `-Xmx` is blocking expansion, oscillation suppression via G1CPUUsageDeviationPercent. (3) ZGCTenuringThreshold `"Computed" (stable)` diagnostic row: upgraded from "no action; healthy" to full deployment-baseline guidance — note stable value in runbook alongside heap size and ZYoungGenerationSizePercent; compare across JVM restarts and canary deployments; a future threshold drop (towards 1) quantifies workload regression severity. (4) G1CollectionSetCandidates `"Maximum number of regions reached"` stopReason: upgraded with full tradeoff explanation — increase `G1OldCSetRegionThresholdPercent` (default 10%) for fewer mixed-GC cycles but longer individual pauses; check `timeRemainingMs` to confirm unused pause budget before raising threshold. (5) GCOverheadLimitExceeded `gcId`: upgraded with null-when-no-GC edge case — null means OOM on the very first allocation (no GC ever completed). (6) GCOverheadLimitExceeded `gcTimePercent`: upgraded with rolling-average interpretation — if gcTimePercent is at limit (98%) but final GC was very short (< 100ms), the rolling average is dominated by earlier slow GCs. (7) GCOverheadLimitExceeded `freeSpacePercent`: upgraded with 3-tier diagnosis (near 0% = fully exhausted; 1–2% = borderline; > 2% at throw time = inconsistent dual-condition). (8) ShenandoahCollectionDecision "What it is used for" tuning patterns: expanded from 7 to 9 bullets — added `triggerType=rate_average` (most common trigger, was entirely absent: steady-state allocation pressure, declining `available` across events signals reclaim rate shortfall) and `triggerType=rate_momentary` (burst absorption, action: increase `ShenandoahMaxYoungGenPercent`); strengthened `rate_accelerated` with `marginOfError` growth interpretation; `expansion_failure` with OOM-imminence formula and `SoftMaxHeapSize` container action.

- **Critique pass 59** (2026-08-19): Vague-language and truncated-description elimination pass — all remaining hedging replaced with deterministic action paths, truncated field descriptions completed. (1) NativeHeapTrim "near-zero after every trim" bullet: replaced duplicate of L183 with distinct guidance — when RSS is not growing, safe to increase `TrimNativeHeapInterval`; when RSS is growing despite near-zero deltaBytes, fragmentation or footprint issue (not interval issue). (2) ShenandoahMMU `gcuPercent > 30%` pattern: replaced "consider increasing -Xmx" with split-by-phase analysis — 30%+ from Concurrent Young → -Xmx; 30%+ from Degenerated Young → concurrent GC failing (check available at trigger, not size); 50%+ concurrent = unsustainable emergency threshold. (3) ShenandoahMMU `gcuPercent trending upward` pattern: removed "likely means" hedge — GCU% growth is definitionally caused by allocation rate outpacing GC throughput; replaced with deterministic causal statement. (4) ShenandoahCollectionDecision `marginOfError`: removed "may be causing" → "is causing" with note that no flags can tune it (purely algorithmic). (5) ShenandoahCollectionDecision `triggerType=fragmentation` pattern: upgraded from "consider whether class unloading helps" to concrete action paths — lower `ShenandoahOldGarbageThreshold` with cost explanation; persistent < 50% → structural fragmentation, `ShenandoahUnloadClassesFrequency` + `-Xmx` as the only remedies. (6) G1CollectionSetCandidates `"Retained candidates exhausted"`: removed "consider raising" → direct action with tradeoff. (7) G1CollectionSetCandidates `overBudgetRegions` pattern: removed "often indicates" → "means" with specific 3-cycle threshold and 2-action (G1MixedGCCountTarget primary, MaxGCPauseMillis secondary). (8) G1CollectionSetCandidates `"Predicted time too high"`: added `timeRemainingMs` confirmation step, explicit "WILL extend pause times" tradeoff statement for MaxGCPauseMillis action, specific G1MixedGCLiveThresholdPercent tradeoff. (9) G1CollectionSetCandidates `"Marking candidates exhausted"`: upgraded healthy-state guidance with concrete marking-start action (lower IHOP or let G1AdaptiveIHOP adapt). (10) G1CollectionSetCandidates `"Region amount reached min"`: upgraded with 3-cycle persistence threshold and full tradeoff explanation for G1MixedGCCountTarget. (11) G1CollectionSetCandidates `Retained type appearing` pattern and `selectedRegions/availableRegions < 0.3` pattern: both upgraded with stop-reason disambiguation and per-case action paths. (12) G1CollectionSetCandidates `availableRegions`: completed truncated description — added low-availableRegions → G1MixedGCLiveThresholdPercent tuning action. (13) G1ConcurrentRefinementPolicy `predictedRefineRate`: completed truncated description — added declining-rate diagnosis and humongous-allocation surge explanation. (14) G1ConcurrentRefinementPolicy `pendingCardsFromGC`: completed truncated description — added 30% GC-sourced threshold and G1MixedGCLiveThresholdPercent + G1OldCSetRegionThresholdPercent actions for GC-internal RS churn. (15) G1ConcurrentRefinementPolicy `timeUntilNextGC`: completed truncated description — added short-interval diagnosis and -Xmx/-allocation-rate actions. (16) G1ConcurrentRefinementPolicy `dirtiedCardRate >> predictedRefineRate × threadsWanted`: upgraded from "expect pause-time spikes" to quantified deficit formula. (17) G1ConcurrentRefinementPolicy `exceededGoal=true, threadsWanted < max`: upgraded with concrete doubling heuristic and thread-ceiling pivot point. (18) GCOverheadLimitExceeded `collector`: upgraded from "identifies which GC" to full analysis-split guidance — G1 vs Parallel field differences, join-event selection, and multi-GC fleet comparison caveat. (19) ZGCTenuringThreshold `reason`: completed truncated description — added per-value action paths and filter-key usage guidance. (20) ZGCTenuringThreshold `"ZTenuringThreshold"` row: removed "may be too conservative" and "usually better calibrated" → "is too high" with concrete consequence and "remove the flag" action. (21) PSAdaptiveSizePolicy `gcDistanceSec`: upgraded from "may be too small" to full edenSizingBranch-gated diagnosis with MaxNewSize and MaxGCPauseMillis action paths. (22) PSAdaptiveSizePolicy `gcDistanceSec very short` pattern: upgraded from "heap may be too small" to edenSizingBranch-cross-reference with two differentiated action paths. (23) G1HeapResize `gcCpuUsageTargetPct`: upgraded from description-only to full use-as-reference guidance — actual/target ratio interpretation with GCTimeRatio tuning actions in both directions.

- **Critique pass 60** (2026-08-19): ZDirectorRule diagnostic pattern upgrades — two short/incomplete pattern bullets completed with action paths. (1) `triggeredMinorRule="_z_allocation_rate"` with decreasing `timeUntilMinorOOM`: added explicit "stalls imminent when timeUntilMinorOOM < GC duration" threshold (0.1–0.5s for ZGC minor) and concrete action (`ZYoungGenerationSizePercent` increase, then `-Xmx`). (2) `triggeredMajorRule="_z_warmup"` in steady state: expanded from "should only fire during initial heap fill" to full semantics — warmup rule threshold explained, production-load occurrence diagnosis (heap oversized for workload), no-action baseline with explicit lower-Xmx exception.

- **Critique pass 61** (2026-08-19): "Why existing events" specificity pass — thin bullets upgraded with field names and concrete gap statements. (1) ShenandoahMMU: G1MMU bullet upgraded with structural distinction ("did the pause stay under goal?" vs. "what fraction of wall-clock time was GC?"); GarbageCollection bullet upgraded with specific missing fields; ShenandoahHeapRegionStateChange and ShenandoahPromotionInformation bullets made distinct. (2) ShenandoahCollectionDecision: ShenandoahHeapRegionStateChange bullet upgraded with missing field names; GCHeapSummary bullet upgraded with specific missing field list; GarbageCollection bullet upgraded with explanation of why cause string is insufficient. (3) ShenandoahReclaimProgress: ShenandoahEvacuationInformation bullet upgraded with "what it says vs. what it doesn't say"; ShenandoahPromotionInformation, GarbageCollection, and "no existing event" bullets upgraded with missing field names and consequence descriptions. (4) ZDirectorRule: ZYoungGarbageCollection, ZOldGarbageCollection, and GarbageCollection bullets upgraded with specific missing fields; final "no existing event" bullet reframed as the core gap statement.

- **Critique pass 75** (2026-08-19): Full log-site audit — every `log_info` and `log_debug` GC source line checked for JFR coverage. Found two production-visible (`log_info`) gaps not covered by any of the 14 existing proposals: (1) `shenandoahOldHeuristics.cpp` — 9 `log_info` lines across `prepare_for_old_collections()`, `finalize_mixed_evacs()`, `top_off_collection_set()`, and `adjust_old_garbage_threshold()` describe the old-gen mixed-evacuation lifecycle with no JFR counterpart → added proposal #15 `jdk.ShenandoahOldEvacuation` (verdict: Propose, priority: High, ~50 lines across 3 sites). (2) `shenandoahAdaptiveHeuristics.cpp:124` — `log_info(gc,ergo)` "Adaptive CSet Selection" in `choose_collection_set_from_regiondata()` has 4 fields (free_target, actual_free, max_cset, min_garbage) with no JFR counterpart → added proposal #16 `jdk.ShenandoahAdaptiveCSetSelection` (verdict: Propose, priority: Medium, ~15 lines). All other `log_info` GC sites are either: covered by existing events (jdk.G1AdaptiveIHOP/jdk.G1BasicIHOP for gc,ergo,ihop; existing GarbageCollection for promotion failure; existing GCHeapConfiguration for ZGC init), covered by existing proposals (#9–14), or low-priority legacy paths (Serial GC expansion failure). No `#ifndef PRODUCT` guards found around any of the newly added emission sites.

- **Critique pass 76** (2026-08-19): G1-specific gap audit — checked all G1 `log_info` sites not covered by existing events or proposals. Found two gaps: (1) `g1ConcurrentMark.cpp` has two `log_info(gc,marking)` sites for concurrent marking interruptions (abort at line ~1282, mark-stack overflow reset at line ~2790) with no JFR event → added proposal #17 `jdk.G1ConcurrentMarkInterrupt` combining both under a `type` discriminator (verdict: Propose, priority: Medium, ~20 lines); GCId OQ documented. (2) `g1CollectedHeap.cpp` has three `log_info(gc,ergo)` sites for Full GC escalation reason (`upgrade_to_full_collection()` line ~954 and `satisfy_failed_allocation_helper()` lines ~1059–1062) — all three carry GC cause `_g1_compaction_pause` making them indistinguishable in `jdk.GarbageCollection` → added proposal #18 `jdk.G1FullGCEscalation` with `escalationPath` discriminator (`upgrade`/`compaction`/`maximal_compaction`), `clearSoftRefs`, `allocationBytes`, `gcId` (verdict: Propose, priority: High, ~25 lines); GCId-timing and `do_full_collection()` signature-change OQs documented.

- **Critique pass 77** (2026-08-19): `jdk.GCOverheadLimitExceeded` JFR write-safety audit. The `satisfy_failed_allocation()` log_info site runs on the VM thread at a safepoint and returns `nullptr` — the OOM is not thrown there. The actual throw is in `MemAllocator::Allocation::check_out_of_memory()` on the mutator thread, outside any safepoint, which throws `Universe::out_of_memory_error_gc_overhead_limit()` (pre-allocated, no heap allocation). JFR write is safe at this site: (1) JFR uses native memory buffers, not Java heap; (2) buffers are pre-allocated per-thread at thread start; (3) mutator thread outside safepoint is the design target for JFR thread-local writes, same as `jdk.ObjectAllocationOutsideTLAB`. Updated emission-point section with full two-phase call chain, added JFR write safety section, updated OQ2 (GCId mechanism) and OQ4 (field scoping via heap singleton) to reflect correct throw site in `memAllocator.cpp`.

## Event Status Summary (as of 2026-08-19, pass 78)

| Event | Priority | Status |
|---|---|---|
| `jdk.NativeHeapTrim` | high | Upstream PR (JDK-8365306) closed Dec 2025; narrow re-proposal recommended; `log_info(trimnative)` ✓; RSS collection gated on logging enabled — JFR impl must decouple |
| `jdk.GCOverheadLimitExceeded` | high | G1 support added JDK 26; no JFR event; two-phase throw: decision in `satisfy_failed_allocation()` (VM thread, safepoint, returns nullptr), actual throw in `MemAllocator::Allocation::check_out_of_memory()` (mutator thread, no safepoint) — JFR emits at throw site; JFR write safe (native buffers, pre-allocated thread-local); `GCOverheadLimitThreshold=5` is a `develop` flag (fixed in prod); all OQs resolved |
| `jdk.ShenandoahMMU` | high | Zero coverage; `log_info(gc,ergo)` in `shenandoahMmuTracker.cpp:107,134` ✓; phase strings confirmed: "Concurrent Young GC", "Degenerated Young GC", etc.; all OQs resolved |
| `jdk.ShenandoahCollectionDecision` | high | **REDESIGN RESOLVED** — verdict upgraded to "Propose with caveats"; split into base event (single control thread site) + `jdk.ShenandoahGCTrigger` follow-up; `decision` field dropped; all OQs resolved |
| `jdk.G1ConcurrentRefinementSweep` | **medium** (↓ from high) | Zero coverage; **`log_debug(gc,refine)`** — below =info threshold; strong production case but debug-tier data |
| `jdk.G1ConcurrentRefinementPolicy` | **medium** (↓ from high) | Zero coverage; **`log_debug(gc,refine)`**; emit from periodic path only (OQ3 resolved); GC-pause path dropped |
| `jdk.G1CollectionSetCandidates` | **medium** (↓ from high) | Zero coverage; **`log_debug(gc,ergo,cset)`** — ALL candidate selection logs are debug level |
| `jdk.ZDirectorRule` | **medium** (↓ from high) | Verdict: "Propose with caveats"; 6-field per-tick summary design; caveats: all-debug source, `ZDirectorResult` struct required for `timeUntilMinorOOM`, emission gate required |
| `jdk.PSAdaptiveSizePolicy` | **medium** (↓ from high) | Zero coverage; **`log_debug(gc,ergo)`**; three-source coordination resolved (single emit at end of `resize_after_young_gc()`) |
| `jdk.G1HeapResize` | medium (unchanged) | Zero coverage; **`log_debug(gc,ergo,heap)`**; deviationCounter field added; sigmoid scaling (steepness=6.0, inflection=1.0) confirmed |
| `jdk.ShenandoahReclaimProgress` | medium | Zero coverage; `log_info(gc,ergo)` in `shenandoahMetrics.cpp:47,58,69,81` ✓; free_space is hard gate (immediate false); ShenandoahCriticalFreeThreshold default = 1%; all OQs resolved |
| `jdk.ShenandoahTenuringThreshold` | medium | Zero coverage; `log_info(gc,age)` in `shenandoahAgeCensus.cpp:258` ✓; algorithm input fields excluded (log_debug — OQ1 resolved) |
| `jdk.ZGCTenuringThreshold` | medium | **REDESIGN RESOLVED** — verdict upgraded to "Propose with caveats"; preferred approach: add `tenuringThresholdReason` field to existing `jdk.ZYoungGarbageCollection`; standalone event is fallback design; all OQs resolved |
| `jdk.ZNMethodRegistration` | low | Weak production motivation; `log_info(gc,nmethod)` in `zStat.cpp:1621` ✓ |
| `jdk.ShenandoahOldEvacuation` | **high** (new) | Zero coverage; `log_info(gc)` / `log_info(gc,ergo)` in `shenandoahOldHeuristics.cpp`; three emission sites: `prepare_for_old_collections()` (garbage survey), `finalize_mixed_evacs()` (outcome), `adjust_old_garbage_threshold()` (threshold auto-tuning); recommended as three separate events (`jdk.ShenandoahOldGarbageSurvey`, `jdk.ShenandoahOldEvacuationOutcome`, `jdk.ShenandoahOldThresholdAdjust`); no prerequisites |
| `jdk.ShenandoahAdaptiveCSetSelection` | **medium** (new) | Zero coverage; `log_info(gc,ergo)` in `shenandoahAdaptiveHeuristics.cpp:124`; single emission site `choose_collection_set_from_regiondata()`; all four fields in scope; ~15 lines |
| `jdk.G1ConcurrentMarkInterrupt` | **medium** (new) | Zero coverage; `log_info(gc,marking)` at two sites in `g1ConcurrentMark.cpp` (abort: line ~1282 in `concurrent_cycle_end()`; overflow reset: line ~2790 in `G1CMTask::handle_abort()`); `type` discriminator distinguishes them; `markCycleCompleted` dropped (always false); ~20 lines |
| `jdk.G1FullGCEscalation` | **high** (new) | Zero coverage; `log_info(gc,ergo)` at three sites in `g1CollectedHeap.cpp` (`upgrade_to_full_collection()` line ~954, `satisfy_failed_allocation_helper()` lines ~1059–1062); `escalationPath` enum distinguishes upgrade/compaction/maximal_compaction; `allocationBytes` and `clearSoftRefs` from existing parameters; `gcCause` dropped (always `_g1_compaction_pause`); GCId requires emit from inside `do_full_collection()`; ~25 lines |
| `jdk.StringDeduplicationStatistics` | — | **REMOVED** — `jdk.StringDeduplication` shipped in JDK 26 (PR #28015, merged Nov 2025) |
| `jdk.ShenandoahCardStatistics` | — | **BLOCKED** — data source is `#ifndef PRODUCT` guarded in `shenandoahCardStats.cpp`; requires moving stats collection to product build first |

## Critique History

- **Critique pass 1** (2026-08-17): Initial 5 events added (NativeHeapTrim, G1ConcurrentRefinement, ZDirectorRule, PSAdaptiveSizePolicy, ShenandoahCollectionDecision)
- **Critique pass 2** (2026-08-17): G1ConcurrentRefinement split into Sweep+Policy; PSAdaptiveSizePolicy averaged/last fields; GCOverheadLimitExceeded diagnostic fields; ZDirectorRule early-exit semantics; ShenandoahMMU priority elevated; ShenandoahReclaimProgress redesigned as flat event; ZNMethodRegistration tableRebuilt added; ShenandoahCardStatistics gated to cumulative-only
- **Critique pass 5** (2026-08-17): Local JDK source audit — function names corrected in G1CollectionSet and G1HeapSizingPolicy; ShenandoahCardStatistics demoted to 'blocked'; jdk.ShenandoahTenuringThreshold promoted (shenandoahAgeCensus.cpp:258 log_info confirmed)
- **Critique pass 6** (2026-08-17): ZGC generational source audit — jdk.ZGCTenuringThreshold promoted; ShenandoahCollectionDecision augmented with trigger fields; wrote zgc-generational.md and shenandoah-heuristics.md
- **Critique pass 7** (2026-08-17): Systematic log level audit — 5 events downgraded from high to medium; GCOverheadLimitExceeded redesigned to OOM throw point only; upstream research confirmed NativeHeapTrim PR closed, StringDeduplication shipped
- **Critique passes 8–9** (2026-08-17): Deep field sourcing — exact variable names, escalation thresholds, G1HeapSizingPolicy sigmoid, G1CollectionSet stopReason strings, PSAdaptiveSizePolicy three-source coordination, ShenandoahReclaimProgress CONSECUTIVE_BAD_DEGEN_PROGRESS_THRESHOLD=2
- **Critique pass 10** (2026-08-17): Links and quotes — all [file.cpp:N] references hyperlinked; exact log message quotes for all 14 events; PSAdaptiveSizePolicy call chain line numbers corrected
- **Critique passes 11–15** (2026-08-17): Source corrections — GCId::peek() semantics fixed (returns _next_id not last id); ShenandoahMMU degenerated phase strings corrected; G1ConcurrentRefinementPolicy two-path field asymmetry documented; ZDirectorRule major-preempts-minor semantics corrected; ShenandoahCollectionDecision decision field corrected to gc_mode_name() values; GCOverheadLimitThreshold=5 is develop flag (fixed in production); ShenandoahCriticalFreeThreshold default=1%; MaxGCPauseMillis default unlimited for Parallel GC; G1RefinementThresholdStep→G1RSetUpdatingPauseTimePercent; jdk.NMethodSweep removed (does not exist); G1ConcRefinementThreads (not G1ConcurrentRefinementThreads)
- **Critique pass 16** (2026-08-18): PSAdaptiveSizePolicy call chain correction — compute_desired_sizes() and compute_old_gen_shrink_bytes() confirmed in same ParallelScavengeHeap::resize_after_young_gc() scope; exact eden/survivor log quote added (psYoungGen.cpp:367)
- **Critique passes 17–20** (2026-08-18): Source code snippets added — is_good_progress() full source (shenandoahMetrics.cpp:38), compute_tenuring_threshold() loop (shenandoahAgeCensus.cpp:264), ZGC select/compute_tenuring_threshold() (zGeneration.cpp:704-816), sigmoid_function() (g1HeapSizingPolicy.cpp:97), stopReason from print_finish_message() (g1CollectionSet.cpp:399), start_gc() early-exit (zDirector.cpp:820), compute_desired_eden_size() 4-branch decision tree (psAdaptiveSizePolicy.cpp:86), execute_trim_and_log() RSS gate (trimNativeHeap.cpp:135), G1 GCOverheadLimitExceeded throw point (g1CollectedHeap.cpp:995-1111), update_utilization() GCU% formula (shenandoahMmuTracker.cpp:86), gc_mode_name() enum mapping (shenandoahGenerationalControlThread.cpp:765)
- **Critique pass 21** (2026-08-18): G1ConcurrentRefinementSweep cardsNoCrossRegion semantic fix — field was described as "same-region reference filter" but actual source (g1RemSet.hpp:122) confirms it counts write-churn cards (mutator changed all cross-region refs AFTER dirtying); tuning diagnosis updated
- **Critique pass 22** (2026-08-18): GCOverheadLimitExceeded field scope fix — long_term_gc_time_ratio and free_space_percent are locals inside update_gc_overhead_counter() and NOT in scope at the throw-point line 1109; must be re-read via accessor calls at emission site
- **Critique pass 23** (2026-08-18): PSAdaptiveSizePolicy field source corrections — promotedBytesEstimate fixed from _avg_promoted->padded_average() to AdaptiveSizePolicy::promoted_bytes_estimate() (adaptiveSizePolicy.hpp:203 = _promoted_bytes.davg() + _promoted_bytes.dsd()); minFreeBytes formula fixed from "10× rate × GC time" to max(padded_avg_promoted, promotion_rate × 600s) with 10-minute lookahead constant
- **Critique pass 24** (2026-08-18): ShenandoahReclaimProgress failedDimension redesign — field had 4 values but is_good_progress() only produces two failure paths: "free_space" (hard gate) and "all_secondary" (all three secondary checks failed); field table, evaluation examples, and tuning actions updated
- **Critique pass 25** (2026-08-18): ShenandoahTenuringThreshold algorithm description corrected from "weighted reciprocal" to accurate scan-down-and-find-oldest-high-mortality description; ShenandoahGenerationalCensusIgnoreOlderCohorts flag semantics documented; GCOverheadLimitExceeded Parallel GC gcTimePercent corrected to gc_time_percent() × 100 (fraction × 100, not 100 - mutator_pct); open question 4 clarified with G1 vs Parallel field-scoping distinction
- **Critique pass 30** (2026-08-18): External references added — Oracle GC tuning doc quotes, JEP links, and JBS/PR references added to each event's "External references" subsection. G1ConcurrentRefinementSweep/Policy: Oracle quotes on Pending Cards and G1RSetUpdatingPauseTimePercent + JEP 522. G1CollectionSetCandidates: Oracle quotes on gc+ergo+cset=debug, G1MixedGCCountTarget, G1MixedGCLiveThresholdPercent, G1HeapWastePercent. G1HeapResize: Oracle GCTimeRatio formula quote (1/(1+GCTimeRatio)=4%). PSAdaptiveSizePolicy: Oracle Parallel GC tuning quotes on throughput/pause goals, priority order, adaptive sizing. ShenandoahMMU/CollectionDecision/ReclaimProgress/TenuringThreshold: JEP 521 links and adaptive heuristics description. ZGCTenuringThreshold + ZDirectorRule: JEP 439 and Oracle ZGC tuning guide. NativeHeapTrim: PR #26756 design discussion. GCOverheadLimitExceeded: JDK-8212084 + PR #27950 description.
- **Critique pass 31** (2026-08-18): Diagnostic pattern tables added — ShenandoahTenuringThreshold "What it is used for" replaced with 5-row pattern table (stable/oscillating/minClamped/maxClamped/sudden-drop). ZGCTenuringThreshold "What it is used for" upgraded with per-`reason` diagnostic table (Computed stable/oscillating/low-threshold, ZTenuringThreshold static, Promote All emergency). Missing "Tuning use" columns added to NativeHeapTrim, GCOverheadLimitExceeded, ZNMethodRegistration, ShenandoahReclaimProgress, ShenandoahCollectionDecision field tables. ZNMethodRegistration "External references" section added (JEP 439 nmethod scanning cost quote). ShenandoahTenuringThreshold algorithm description corrected from "weighted reciprocal" to scan-down mortality analysis. ShenandoahMMU periodSeconds description fixed from tautology to CPU-time formula. G1HeapResize field descriptions augmented with GCTimeRatio=24 worked example. PSAdaptiveSizePolicy throughput/gcDistance field descriptions clarified with Parallel GC default GCTimeRatio=4 context. G1CollectionSetCandidates "What it is used for" upgraded with stopReason → tuning action mapping table.
- **Critique pass 32** (2026-08-18): JFR metadata audit against `src/hotspot/share/jfr/metadata/metadata.xml`. Key findings: (1) `jdk.ZYoungGarbageCollection` already has a `tenuringThreshold` field — ZGCTenuringThreshold verdict changed from "Propose" to "Redesign first"; preferred upstream approach is adding `reason` field to existing event. (2) `jdk.GCCPUTime` explicitly does not cover Shenandoah (metadata description + no GCTraceCPUTime in Shenandoah paths) — added to ShenandoahMMU "Why existing events" as explicit gap evidence. (3) `jdk.ShenandoahEvacuationInformation` and `jdk.ShenandoahPromotionInformation` exist — added to "Why existing events" sections for ShenandoahCollectionDecision, ShenandoahReclaimProgress, ShenandoahTenuringThreshold to show no overlap. (4) `jdk.ZStatisticsCounter`/`ZStatisticsSampler` are experimental (confirmed) — added to ZDirectorRule. (5) `jdk.ZAllocationStall` added as complementary reference in ZDirectorRule (failure-case vs. prevention-side). (6) `jdk.OutOfMemoryError` does NOT exist in JFR — corrected from "(hypothetical)" to factual statement; added GCCPUTime to GCOverheadLimitExceeded "Why existing events" section.
- **Critique pass 33** (2026-08-18): "The question it answers" sharpening pass. (1) GCOverheadLimitExceeded: removed stale `jdk.OutOfMemoryError (if it exists)` phrase; reframed around "no JFR event fires at the GC decision point" and impossibility of post-hoc analysis on OOM. (2) G1CollectionSetCandidates: corrected "start or stop mixed GCs" to "selection stopping within an already-running mixed GC"; added concrete stopReason framing so readers understand what question cannot be answered today. (3) G1HeapResize: clarified that delta is computable from G1HeapSummary diffs but policy drivers (deviation counter, thresholds, scale factor) are not; removed misleading "partial redundancy" implication. (4) G1ConcurrentRefinementSweep: reframed from throughput description to backlog→pause-time impact ("residual backlog must be drained during the next GC pause under G1RSetUpdatingPauseTimePercent budget"). (5) ZGCTenuringThreshold fields table: `tenuringThreshold` field annotated as redundant with existing `jdk.ZYoungGarbageCollection` field; `reason` field annotated as the unique value of the proposal.
- **Critique pass 34** (2026-08-18): Precision and concrete-example pass. (1) ZNMethodRegistration "The question it answers": reframed from "how many nmethods" to scanning-overhead impact and staleness detection. (2) PSAdaptiveSizePolicy "The question it answers": added the oscillating-policy failure mode ("eden grows, pauses spike, eden shrinks, throughput drops, repeat") as the uniquely undetectable case without this event. (3) G1ConcurrentRefinementPolicy "What it is used for": added concrete numeric worked example (`dirtiedCardRate=500 cards/ms`, `predictedRefineRate=200 cards/ms × 2 threads = 400 cards/ms < demand`) showing how the imbalance is invisible below debug-log level. (4) ZDirectorRule fields table: `triggeredMinorRule` nullable semantics clarified — null means "evaluated/nothing triggered" OR "not evaluated because major fired"; disambiguated via `triggeredMajorRule != null`. Open question 4 (which documented this ambiguity) removed as it is now in the fields table.
- **Critique pass 35** (2026-08-18): Field precision and open-question resolution pass. (1) NativeHeapTrim `deltaBytes`: sign semantics (negative = returned to OS, positive = allocation outpaced reclaim) moved into field table Source column; redundant prose paragraph removed. (2) PSAdaptiveSizePolicy `desiredSurvivor` field: expanded from tautology to concrete survivor/eden comparison use; `survivorOverflow` co-diagnosis note added. (3) PSAdaptiveSizePolicy diagnosis patterns: oscillating-policy pattern added to the bulleted list (alternating `throughputEdenIncrease` and `minorPauseMs > pauseGoalMs`). (4) PSAdaptiveSizePolicy `throughputEdenIncrease`: annotated as candidate for replacement by `edenSizingBranch` string; open question 3 rewritten as a concrete 5-value recommendation. (5) ZDirectorRule "The question it answers": corrected "ZGC was never evaluated" (wrong — director always evaluates); reframed around silence on no-trigger ticks. (6) ZDirectorRule diagnostic patterns: major-preempts-minor null semantics surfaced in the `triggeredMajorRule="_z_allocation_rate"` pattern. (7) ShenandoahMMU "The question it answers": added `jdk.G1MMU` field names (`gcTime` vs `pauseTarget`) to make the structural difference concrete. (8) G1CollectionSetCandidates `availableRegions` field: explained what determines the candidate count (`G1MixedGCLiveThresholdPercent` liveness filter) and what low `availableRegions` implies. (9) ShenandoahCollectionDecision `cause` field: added cause-value semantics; `available` field: added fill-fraction formula and `ShenandoahMinFreeThreshold` tuning note. (10) G1HeapResize open question 4 (`gcCpuUsageTargetPct`): resolved to "keep" with justification that `scale_with_heap()` makes the effective target non-derivable from `GCTimeRatio` alone.
- **Critique pass 36** (2026-08-18): "Why existing events" field-specificity audit. Five sections upgraded from generic "no event covers X" to field-name-level claims: (1) GCOverheadLimitExceeded: `jdk.GCCPUTime` expanded with specific missing fields (`_gc_overhead_counter`, `long_term_gc_time_ratio`, `GCHeapFreeLimit`); `jdk.GCHeapSummary` expanded to name the outcome-only fields it carries. (2) ShenandoahTenuringThreshold: added `jdk.GarbageCollection` explanation (cause string carries no mortality-rate data); `jdk.TenuringDistribution` expanded to explain why the age distribution input ≠ the threshold output. (3) G1ConcurrentRefinementSweep: `jdk.EvacuationInformation` and `jdk.G1AdaptiveIHOP` expanded with their actual fields; added final bullet listing the specific fields only the new event provides. (4) G1CollectionSetCandidates: `jdk.EvacuationInformation` expanded with its actual fields (`cSetRegions`, `cSetUsedBefore`, etc.); `jdk.G1HeapSummary` and `jdk.GarbageCollection` added; final bullet lists absent fields by name. (5) G1HeapResize: `jdk.G1AdaptiveIHOP` expanded with its actual fields; `jdk.G1HeapSummary` expanded; `jdk.GCConfiguration` added. (6) PSAdaptiveSizePolicy: `jdk.PSHeapSummary` expanded with its actual fields; `jdk.GCConfiguration` added; final bullet lists absent per-cycle decision fields by name.
- **Critique pass 37** (2026-08-18): "Why existing events" precision pass (continued). (1) G1ConcurrentRefinementPolicy: replaced generic "same analysis" placeholder with specific missing fields — `threadsWanted` and `pendingCardsTarget` named as the unique outputs of `adjust_threads_wanted()` not observable in any existing event. (2) ZNMethodRegistration: `jdk.CodeCacheStatistics` expanded with its actual fields (`entryCount`, `methodCount`, `adaptorCount`, `unallocatedCapacity`) and explicitly distinguished from `ZNMethodTable` — a separate data structure; `_nunregistered` stale slots and per-GC scan costs named as absent. (3) NativeHeapTrim: `jdk.ResidentSetSize` expanded with its actual fields (`size`, `peak`), its `period="everyChunk"` cadence, and a precise statement of what it cannot do — no `deltaBytes` field, no trim attribution, no single-operation isolation. (4) NativeHeapTrim `jdk.ResidentSetSize` stale version qualifier "(JDK 27 master)" removed from "The question it answers"; "(JDK 27)" removed from "External references" — event already present in local metadata.xml. (5) ShenandoahTenuringThreshold: removed duplicate `jdk.GarbageCollection` bullet (identical content appeared twice).
- **Critique pass 38** (2026-08-18): Open-question resolution pass. (1) NativeHeapTrim OQ2 resolved: `deltaBytes` as signed `long` is the right design — `os::size_change_t` has unsigned `before`/`after`; `deltaBytes = (long)afterBytes - (long)beforeBytes` is computable at emit time. OQ3 resolved: `_num_trims_performed` confirmed cumulative from JVM init at `trimNativeHeap.cpp:169`. (2) ShenandoahMMU: `gcuPercent`/`muPercent` source formula expanded — `_active_processors` is fixed at JVM startup via `os::initial_active_processor_count()`; container CPU quota changes post-startup are not reflected. `periodSeconds` corrected to "wall-clock since previous call to `update_utilization()`", not "GC phase duration". OQ2 resolved: `gcId` from `record_*` callers is reliable; `record_old_marking_increment()` intentionally excluded (rolls up into next cycle). (3) ZGCTenuringThreshold OQ3 resolved: `gcId` reliable at `select_tenuring_threshold()` time — `GCIdMark` set in `ZDriverScopeMinor` before `collect()` is called. (4) G1HeapResize OQ3 resolved: event should gate on `resizeBytes != 0` — `bool& expand` reference is only assigned inside expand/shrink branches; stale when neither fires. `expand` field description updated with this note. (5) PSAdaptiveSizePolicy OQ4 resolved: both `gcDistanceSec` and `gcDistanceSecLast` are needed — `compute_desired_eden_size()` uses `.last()` as the branching input; `.davg()` shows the trend.
- **Critique pass 40** (2026-08-18): Resolve remaining OQs and fix design issues. (1) GCOverheadLimitExceeded: `consecutiveViolations` field recommended to drop — always 5 at throw time (`develop` flag constant), zero information value; field table and "What it is used for" updated accordingly. OQ3 resolved. (2) PSAdaptiveSizePolicy: `throughputEdenIncrease` (nullable) replaced with `edenSizingBranch` string enum (`"throughput_grow"`, `"pause_shrink"`, `"distance_grow"`, `"distance_shrink"`, `"unchanged"`) in field table; oscillation diagnosis pattern updated. OQ3 resolved. (3) G1ConcurrentRefinementPolicy: OQ3 resolved — recommend periodic-path-only emission; `pendingCardsTimeMs` marked recommended-to-drop if periodic-only adopted; `predictedPendingCards` upgraded from "Consider dropping" to "Keep — resolved". (4) G1HeapResize: OQ4 marked resolved (keep `gcCpuUsageTargetPct` due to `scale_with_heap()` modifier). (5) G1ConcurrentRefinementSweep: removed incorrect JEP 522 reference ("Ahead-of-Time Class Loading & Linking") throughout — that JEP has nothing to do with G1 refinement; replaced with references to JDK-8382089/JDK-8383794 state-machine redesign. (6) ShenandoahMMU: `muPercent` clarified — > 100% is a CPU accounting boundary artifact, treat as ≈ 100%; improved idle-state interpretation. (7) ZNMethodRegistration: OQ1 strengthened to require concrete evidence before upstream filing (benchmark data, bug report, or GraalVM case study).
- **Critique pass 41** (2026-08-18): Resolve all remaining open questions with source verification. (1) G1ConcurrentRefinementSweep OQ2: `cardsNotParsable` description corrected to `CouldNotParse` retry indicator (source: `g1ConcurrentRefineSweepTask.cpp:75-79`; card re-dirtied with original value, not a "mid-transition region"); `cardsRefersToCset` dropped (intermediate `HasRefToCSet` — never survives to mixed-GC use); `cardsStillRefersToCset` description corrected to `AlreadyToCSet` semantics (source: line 66-69). (2) G1CollectionSetCandidates OQ2: concrete `stopReason` enum implementation (~10 lines); OQ3: two-event design recommended (Marking+Retained, per-mixed-GC pair); OQ4: drop `availableGroups` + `predictedOptionalTimeMs`. (3) G1HeapResize OQ2: accept shrink-path nullable fields with documented null condition. (4) G1ConcurrentRefinementPolicy: complete periodic-path-only redesign — all GC-pause-path nullability removed from fields table; `pendingCardsTimeMs` dropped; `threadsWanted` no longer nullable; cleaner descriptions throughout. (5) GCOverheadLimitExceeded OQ1: recommend combined event with nullable GC-type fields; OQ2: `gcId` resolved to `GCId::peek()-1` with thread-safety note. (6) ZGCTenuringThreshold OQ1: firm field-extension recommendation for `jdk.ZYoungGarbageCollection` with PR rationale. (7) ShenandoahCollectionDecision OQ3: `decision` field recommended to drop from initial proposal (always "normal"). (8) ShenandoahReclaimProgress: all four OQs resolved. (9) ZDirectorRule OQ3: concrete `ZDirectorResult` struct sketch for `timeUntilMinorOOM` propagation. (10) NativeHeapTrim OQ1: strengthened with blast-radius argument and NMT precedent.
- **Critique pass 42** (2026-08-18): Terminology fixes and OQ strengthening. (1) PSAdaptiveSizePolicy: fix stale reference to dropped `throughputEdenIncrease` field in code comment below eden decision tree — replaced with `edenSizingBranch` description. (2) G1ConcurrentRefinementPolicy `startTime` description: updated for periodic-path-only model — fires between pauses, not at GC pause boundaries. (3) G1CollectionSetCandidates `stopReason`: distinguished Marking-path stop reasons (via `print_finish_message()`) from Retained-path stop reason (`"Retained candidates exhausted"` is a direct `log_debug` at line 613, not via `print_finish_message`); clarified that when the retained loop exits early there is no explicit stop-reason log — `timeRemainingMs == 0` indicates budget exhaustion. (4) ZDirectorRule `timeUntilMinorOOM` nullable semantics clarified: null means the dynamic alloc-rate rule was not the firing rule (major preempted evaluation or a different minor rule fired) — not just "not evaluated". (5) ZNMethodRegistration OQ2 resolved: emit for both young and old collections with justification (shared nmethod table, `staleNMethodSlots` accumulation pattern is per-young-cycle). (6) All debug-level OQ1 items strengthened: G1ConcurrentRefinementSweep/Policy/CollectionSetCandidates/HeapResize and PSAdaptiveSizePolicy now each carry a concrete recommended approach — promote relevant log calls to `log_info` in the same PR as the JFR event; cites `jdk.G1AdaptiveIHOP`/`jdk.G1BasicIHOP` as the precedent (those events also come from `log_debug(gc,ergo)` sites). (7) ZDirectorRule OQ1 strengthened: propose log-level promotion of `timeUntilMinorOOM` log to `log_info` alongside the JFR event — it is ZGC's primary OOM-prevention metric and deserves info-level visibility. (8) G1ConcurrentRefinementPolicy OQ1 strengthened with specific argument: `threadsWanted` is the output of a control loop that directly affects GC pause time — excluding it from info-level logging while documenting it as a tuning control is an observability inconsistency.
- **Critique pass 43** (2026-08-18): Field description strengthening pass — every field now names exactly when it fires, what formula to apply, and which events to join. (1) NativeHeapTrim `afterBytes`: expanded to include `malloc_trim()` attribution and "difference from `jdk.ResidentSetSize`" usage guidance. (2) ShenandoahMMU `gcId`: named `GCIdMark` callsites and expanded join guidance with `jdk.ShenandoahCollectionDecision`. (3) ShenandoahCollectionDecision `startTime`: correlate with `jdk.ShenandoahMMU` end-of-cycle to measure full cycle wall time. (4) ShenandoahCollectionDecision trigger fields `currentUsageBytes`: added per-trigger interpretation formulas (growth rate formula for `growth` trigger; near-`-Xmx` OOM signal for `expansion_failure`). (5) ShenandoahTenuringThreshold `gcId`: named `GCIdMark` callsite; expanded join to include `jdk.ShenandoahCollectionDecision`. (6) ZGCTenuringThreshold `gcId`: named `GCIdMark` in `ZDriverScopeMinor`; added "Promote All" old-gen spike diagnostic. (7) G1ConcurrentRefinementSweep `duration`: added saturation signal; `cardRefineMs`: clarified it measures only `refine_card_concurrently()` CPU time and added throughput formula `cardsNotClean / cardRefineMs`. (8) ZDirectorRule `startTime`: replaced "Tick frequency check" with gap-analysis guidance and `ZDirectorPeriod` reference; `minorFreeBytes`/`majorFreePercent`: added source rule names and `timeUntilMinorOOM` derivation formula. (9) G1HeapResize `startTime`: added emission-gate context (events sparse, `resizeBytes != 0`); `startTime` join to `gcId`. (10) PSAdaptiveSizePolicy `startTime`: named fire point; `oldGenFree`: expanded headroom semantics; `minFreeBytes`: added decreasing pattern; `shrinkBytes`: added shrink-then-overflow risk. (11) GCOverheadLimitExceeded: `startTime` named OOM throw point; `gcId` expanded join guidance; `collector` noted as null-gating field; `freeSpaceYoungPercent`/`freeSpaceOldPercent` got old-vs-young comparison diagnostic and `NewRatio` tuning action. (12) ShenandoahReclaimProgress `startTime`: named `is_good_progress()` evaluation point. (13) ShenandoahTenuringThreshold `startTime`: fires during collection preparation, before CSet finalization. (14) ZNMethodRegistration `startTime`: named `ZStatNMethods::print()` callsite and `registeredNMethods` trend pattern. (15) G1CollectionSetCandidates `startTime`: two events per mixed GC pause with same `gcId` documented.
- **Critique pass 45** (2026-08-18): Schema cleanup + redundancy audit pass — metadata.xml verified against all 16 events. (1) GCOverheadLimitExceeded.`consecutiveViolations` REMOVED from JSON schema — always equals `GCOverheadLimitThreshold` (5, a `develop` flag not configurable in production), zero information value at throw time; doc already recommended dropping it, JSON now matches. (2) PSAdaptiveSizePolicy.`throughputEdenIncrease` REPLACED with `edenSizingBranch` enum in JSON — non-null branch label (`throughput_grow`/`pause_shrink`/`distance_grow`/`distance_shrink`/`unchanged`), raw delta derivable from `jdk.PSHeapSummary`; doc had already been updated in pass 44, JSON now consistent. (3) ShenandoahReclaimProgress: simplified 5-field upstream version added as primary fields in JSON (`freePercent`, `goodProgress`, `failedDimension`, `badProgressCount`) with the 12-field nullable design preserved as reference; `badProgressCount` was missing from JSON despite being the most actionable field. (4) ZGCTenuringThreshold status updated to "REDESIGN FIRST" in JSON — `jdk.ZYoungGarbageCollection` already has `tenuringThreshold` field (confirmed in metadata.xml); preferred approach is adding `tenuringThresholdReason` to existing event; only propose standalone if timing granularity is a distinct requirement. (5) ShenandoahCollectionDecision.`decision` field marked "RECOMMENDED TO DROP for initial proposal" in JSON — always `"normal"` at the `service_concurrent_normal_cycle()` emission point. (6) Web research via gh API: JDK `metadata.xml` confirmed at 196 GC events; `jdk.ShenandoahPromotionInformation` confirmed in master; no new GC events added that close any of our proposal gaps.
 — replaced "high value means..." and "watch for..." language with specific thresholds, formulas, and named flag actions. (1) ShenandoahMMU `gcuPercent`: added explicit healthy ranges (< 10% latency-sensitive, < 30% batch) and container CPU-quota staleness caveat; `muPercent`: clarified idle indicator pattern with example sum < 60%. (2) ShenandoahCollectionDecision `cause`: added urgency classification with flags (`ShenandoahMinFreeThreshold`, `-Xmx`) per cause value; `available`: filled-in fill-fraction interpretation with explicit percentages (> 30% healthy, < 10% late trigger). (3) ShenandoahReclaimProgress `failedDimension`: mapped each failure value (`free_space`, `all_secondary`) to a distinct tuning action with specific flags (`-Xmx`, `ShenandoahGarbageThreshold`, `ShenandoahUnloadClassesFrequency`). (4) ZNMethodRegistration `registeredNMethods`: added typical ranges (5–15K standard, 20–100K+ polyglot) and scan-overhead threshold (> 5% of collection); `staleNMethodSlots`: added ratio threshold (> 10%), root causes (deoptimization, code-cache eviction), and `jdk.Deoptimization` cross-reference. (5) G1ConcurrentRefinementSweep `cardsNotParsable`: replaced "typically humongous" with specific threshold (≥ 3 consecutive sweeps) and humongous-region diagnosis path. (6) G1ConcurrentRefinementPolicy `threadsWanted`: clarified "wants vs. has" distinction and added low-threadsWanted-but-high-backlog misalignment diagnosis; `predictedPendingCards`: added 3 specific actions when capacity is exceeded (reduce cross-region writes, increase pause frequency, increase `-XX:G1RSetUpdatingPauseTimePercent`). (7) G1CollectionSetCandidates `stopReason`: added healthy-state guidance ("exhausted is healthy") and constraint-classifier framing with per-value flag mapping. (8) G1HeapResize `scaleFactorPct`: added sigmoid aggressiveness formula, risk pattern (high scale → promotion spike → overflow); `freeRegions`: added fragmentation diagnosis for low-freeRegions-despite-free-space. (9) ZDirectorRule `timeUntilMinorOOM`: added explicit urgency thresholds (> 10s safe, < 2s stalls imminent), trend interpretation, and `-XX:ZYoungGenerationSizePercent` action; `majorFreePercent`: replaced `~10–20%` with specific threshold tiers and per-tier interpretation; `minorFreeBytes`: added decline-rate threshold (> 10% per tick). (10) PSAdaptiveSizePolicy `pauseGoalMs`: added "effective disabled" note for default value and oscillation-risk cross-reference; `edenSizingBranch`: added per-branch interpretation including `distance_grow`/`distance_shrink` which were previously undescribed.
- **Critique pass 47** (2026-08-18): Field description precision and "vague action" removal pass. (1) NativeHeapTrim `startTime`: replaced "Verify trim fires at expected interval" with concrete inter-trim interval computation and diagnosis for intervals exceeding `TrimNativeHeapInterval` (OS scheduling pressure, trim-serialization condition). (2) NativeHeapTrim `duration`: replaced "> 100ms suggests heavy fragmentation" with three-tier thresholds (< 10ms normal, 10–100ms elevated with root causes, > 100ms significant with specific OS causes) and trim-serialization condition when `duration > TrimNativeHeapInterval`. (3) NativeHeapTrim `trimCount`: added explicit gap-check formula (`trimCount` difference should equal 1 between consecutive events; difference ≥ 2 indicates a dropped event) with JFR buffer action. (4) ShenandoahMMU first key pattern ("Check heap sizing"): replaced with actionable branching diagnosis — cross-reference `jdk.ShenandoahCollectionDecision` `triggerType` to distinguish steady growth (`rate_average`) from burst (`rate_accelerated`); `muPercent` trending together with `gcuPercent` indicates memory-intensive workload vs. `muPercent` flat while `gcuPercent` rises indicates efficiency degradation (check fragmentation trigger). (5) ShenandoahMMU second key pattern ("young gen undersized"): added specific flags `ShenandoahMinYoungGenPercent`/`ShenandoahMaxYoungGenPercent` and cross-reference to `jdk.ShenandoahCollectionDecision` `available` field verification. (6) ShenandoahCollectionDecision mixed GC fields: all six fields upgraded from one-liners to substantive tuning use descriptions — `immediateGarbage`: added efficiency ratio interpretation and "all arenas committed" state; `immediateRegions`: added `liveAtPrevMarkBytes` growth correlation and `ShenandoahOldGarbageThreshold` action; `mixedCandidates`: added "zero candidates" diagnosis with `ShenandoahOldGarbageThreshold` action; `mixedRegionsSelected`: concrete disambiguation using `oldEvacuationBudget` (budget-constrained vs. fully-consumed); `oldEvacuationBudget`: added frequency diagnosis; `defragRegions`: added budget-competition diagnosis with `fragmentationDensityPct` cross-reference. (7) G1ConcurrentRefinementSweep `cardsStillRefersToCset` key pattern: replaced "consider adjusting CSet selection" with specific action — lower `G1MixedGCLiveThresholdPercent` to exclude regions with high reference fan-in, with tradeoff statement. (8) G1CollectionSetCandidates `selectedRegions`: replaced ambiguous "low ratio means time budget or maxRegions was the binding constraint" with three-case disambiguation using `stopReason` + `timeRemainingMs` — case 1 (`time too high` + `timeRemainingMs == 0`), case 2 (`max regions` + `timeRemainingMs > 0`), case 3 (`exhausted`, healthy); note that the ratio alone is insufficient for disambiguation.
- **Critique pass 48** (2026-08-18): Per-value interpretation and differentiated action pass. (1) ZDirectorRule `triggeredMajorRule`: replaced bare value list with per-value interpretation — `_z_warmup` in steady state = miscalibration; `_z_proactive` with `ZProactiveGCInverseGoodness` tuning flag; `_z_allocation_rate` as major = young gen undersized with `-XX:ZYoungGenerationSizePercent` action; `_z_timer` = not a pressure signal. (2) ShenandoahMMU `phase` field: replaced "Full/degenerated typically higher GCU%" with concrete benchmarks (concurrent young 5–15%, Full/Degenerated 50–100%), low-GCU Full GC diagnosis (NUMA/I/O), and high-GCU degenerated diagnosis (concurrent GC was seriously behind); added phase-pair comparison value quantifying fallback overhead cost. (3) ZNMethodRegistration tuning actions: replaced two thin bullets with three differentiated bullets — stale ratio > 10% → deoptimization or TI agent investigation with `jdk.Deoptimization` cross-reference; high registeredNMethods → tier-3 JIT and `ReservedCodeCacheSize` actions; stale never zero → rebuild suppression diagnosis. (4) ShenandoahReclaimProgress `all_secondary` tuning action: differentiated by underlying sub-failure — used-space failure → lower `ShenandoahGarbageThreshold`; internal/external frag failure → increase `ShenandoahUnloadClassesFrequency`; added note that simplified 5-field event cannot distinguish sub-dimensions (only full 12-field version can). (5) GCOverheadLimitExceeded "What it is used for": split G1 and Parallel GC interpretations; Parallel section adds three-case `freeSpaceYoungPercent`/`freeSpaceOldPercent` decision matrix — young near 0 but old has space = promotion failure → `NewRatio` action; old near 0 = old gen exhausted → `-Xmx` action; both near 0 = full heap exhaustion. (6) PSAdaptiveSizePolicy `gcDistanceSecLast`: replaced "sudden drop = single fast GC / sustained difference = workload changed" with policy-variable annotation (`.last()` is the actual branch input, not `.davg()`), bimodal pattern threshold (`gcDistanceSecLast < gcDistanceSec / 3` for ≥ 3 events), and `promotedBytesLast` cross-reference for burst detection.
- **Critique pass 49** (2026-08-18): Field precision and consequence pass — thin fields upgraded with missing action paths. (1) ShenandoahReclaimProgress `freePercent`: added formula, three-tier threshold (< 0.5% OOM-imminent, 1–3% danger zone, > 10% on `goodProgress=false` means compaction failure not capacity); `goodProgress`: expanded to document `true` case — `badProgressCount` reset to 0, escalation chain cleared; `false` case unchanged. (2) ShenandoahTenuringThreshold `minTenuringAge`/`maxTenuringAge`: both now document the consequence when the clamp binds (`min` = young-gen pressure from held-back promotions; `max` = survival pressure from long-lived objects accumulating in young gen) and the action including risk-awareness. (3) ShenandoahTenuringThreshold oscillation pattern: `ShenandoahGenerationalTenuringMortalityRateThreshold` direction explained — raising = less sensitive (fewer threshold drops on transient bursts), lowering = more sensitive (wider oscillation). (4) G1CollectionSetCandidates `optionalRegions`: two-case interpretation (always-0 with `timeRemainingMs == 0` = time-constrained; always-0 with `timeRemainingMs > 0` = pool exhausted); non-zero with time-based `stopReason` = Retained-list accumulation pattern. (5) G1CollectionSetCandidates `overBudgetRegions`: added cross-reference to `predictedInitialTimeMs` vs. actual `jdk.GarbageCollection` duration for verifying whether over-budget additions cause real pause extension; action: decrease `G1MixedGCCountTarget` to spread work. (6) G1CollectionSetCandidates `predictedInitialTimeMs`: added calibration check (consistently under 20% = model underestimating; over = conservative) and budget-health formula (`< MaxGCPauseMillis × (1 - 1/G1MixedGCCountTarget)`). (7) G1HeapResize `atLimit`: documented `expand=false + atLimit=true` case — heap at minimum, shrink wanted but blocked; common causes: `-Xms == -Xmx`, OS huge-page backing, `InitialHeapSize == MaxHeapSize`; action: reduce `-Xms` if OS memory pressure is a concern. (8) G1ConcurrentRefinementPolicy `pendingCards`: added rate-of-growth heuristic — > 20% increase between consecutive events = refinement threads not draining; `pendingCardsTarget`: added derivation formula and `G1RSetUpdatingPauseTimePercent` tuning guidance with 25% upper bound warning.
- **Critique pass 50** (2026-08-18): Verdict update + structural fix pass. (1) ZDirectorRule verdict upgraded from "Redesign first" to "Propose with caveats" — the 6-field per-tick summary redesign is complete; three remaining caveats documented (all-debug source, struct accumulation, emission gate). Status table entry updated accordingly. (2) ShenandoahCollectionDecision "Tuning patterns": removed three `decision=` pattern bullets that referenced the dropped `decision` field (OQ3 had resolved to drop it); replaced with `generation=` + `triggerType=` cross-pattern bullets that work with the actual proposed fields — including `generation="Old"` with `triggerType=growth` old-gen overproduction pattern, `generation="Young"` alternating with `generation="Old"` fragmentation pattern, and `jdk.ShenandoahReclaimProgress` `badProgressCount` cross-reference for escalation-chain detection. (3) ShenandoahCollectionDecision OQ2: expanded trigger-field separation from a stub to a concrete `jdk.ShenandoahGCTrigger` proposal sketch — fields listed (`gcId` join key, `triggerType`, 7 type-specific nullable fields), thread assignment (regulator thread only, avoiding multi-thread emission problem), and recommended filing strategy (base event first, trigger event as follow-up). (4) G1ConcurrentRefinementSweep `cardsStillRefersToCset`: upgraded from "Non-zero and growing → may increase Update RS work" to a complete diagnostic — `> 5%` threshold for two consecutive sweeps, root-cause explanation (high-reference-fan-in regions in CSet), and `G1MixedGCLiveThresholdPercent` action with tradeoff statement.
- **Critique pass 51** (2026-08-18): Bug fix + field guidance precision pass. (1) PSAdaptiveSizePolicy `survivorOverflow` **BUG FIX** — corrected tuning direction: "increase `SurvivorRatio`" was wrong; `-XX:SurvivorRatio` is the eden-to-survivor ratio (default 8 = eden 8× survivor), so a *lower* value gives survivor *more* space; fixed in both the field table row and the "Diagnosis patterns" bullet. (2) ShenandoahMMU `startTime`: added join guidance — correlate with `jdk.GarbageCollection` on `gcId` to join GCU%/MU% against pause duration; `periodSeconds` covers full inter-GC wall-clock; `jdk.GarbageCollection.duration` covers the STW portion; difference = concurrent phase wall-clock time. (3) NativeHeapTrim "Tuning patterns": expanded from 3 to 5 bullets — distinguished `detailsAvailable=true` near-zero (arenas committed vs. fragmentation pinning segments, diagnosed by heap usage growth) from `detailsAvailable=false` (platform limitation); added continuously-large deltaBytes (> 20MB every trim) → decrease `TrimNativeHeapInterval`. (4) ZDirectorRule `triggeredMinorRule`: `"_z_timer"` value documented distinctly as periodic timer with no allocation pressure (healthy cadence, confirm via `heapFreePercent` trend), contrasted with `"_z_allocation_rate"` (rate-model pressure signal) and `"_z_high_usage"` (occupancy safety net). (5) G1ConcurrentRefinementPolicy `goalMs`: completed action guidance when goalMs is too small — raise `G1RSetUpdatingPauseTimePercent` (proportional effect, cap at 25%), raise `MaxGCPauseMillis`; documented the fundamental write-rate ceiling case where no tuning helps and application-level cross-region write reduction is required. (6) ShenandoahTenuringThreshold `tenuringThreshold` oscillation: rewritten as three-case decomposition — (a) min-to-mid oscillation: raise `ShenandoahGenerationalTenuringMortalityRateThreshold` to reduce sensitivity; (b) mid-to-max oscillation: informational, reflects workload structure, check old-gen occupancy trend; (c) raising `ShenandoahGenerationalMaxTenuringAge` only helps when the high end of oscillation is clamped at the current max.
- **Critique pass 52** (2026-08-18): Field precision pass — thin card and region fields upgraded. (1) G1ConcurrentRefinementSweep `cardsScanned`: upgraded from "volume indicator" to full denominator documentation — equals sum of all card-category counters; `cardsScanned / duration` = sweep throughput diagnostic; small `cardsScanned` with large `cardsPending` = cards arriving faster than sweeps. (2) G1ConcurrentRefinementSweep `cardsClean`: upgraded with `> 50%` threshold, two-cause disambiguation (GC-phase cleanup vs. thread over-count), and resolution via `threadsWanted` vs. `G1ConcRefinementThreads` comparison. (3) G1ConcurrentRefinementSweep `cardsPending`: upgraded with 3-consecutive-sweep growth threshold and cross-reference to `pendingCardsTarget` from Policy event for spill-to-pause prediction. (4) G1ConcurrentRefinementSweep `preSweepMs`: added `> 25%` of `cardRefineMs` threshold, three root causes (lock contention, large RS structures, high region count), and `G1HeapRegionSize` action. (5) G1ConcurrentRefinementPolicy `exceededGoal`: replaced single-bullet with three-branch resolution tree (threads below max → increase `G1ConcRefinementThreads`; threads at max with small `goalMs` → raise `G1RSetUpdatingPauseTimePercent` or `MaxGCPauseMillis`; threads at max with adequate `goalMs` → fundamental write-rate ceiling). Key patterns bullet updated to match. (6) G1CollectionSetCandidates `minRegions`: corrected description — documents the over-budget floor mechanism and its interaction with `overBudgetRegions`; added `G1MixedGCCountTarget` action for reducing over-budget additions. (7) G1CollectionSetCandidates `maxRegions`: added derivation formula, binding-constraint case (`selectedRegions == maxRegions` + `timeRemainingMs > 0`), and both directions of tuning `G1OldCSetRegionThresholdPercent`. (8) G1CollectionSetCandidates `timeRemainingMs`: upgraded to constraint classifier — 0 vs. positive branches with `stopReason` cross-reference and `overBudgetRegions` interaction. (9) G1HeapResize `regionsNeededForAlloc`: upgraded from "safety floor" to full actionability — when `freeRegions ≈ regionsNeededForAlloc`, shrink is blocked by allocation headroom; diagnosis vs. deviation-threshold blocked shrink; action is reducing allocation pressure, not resize parameters.
- **Critique pass 53** (2026-08-18): Per-value precision pass — thin trigger and generation fields upgraded. (1) G1HeapResize `shortTermGcCpuUsagePct`: upgraded from "compare against thresholds" to trend interpretation — sustained above `upperThresholdPct` triggers expansion after 4 events; sustained below `lowerThresholdPct` triggers shrink after 8 events; oscillating without sustained direction = bursty workload, no resize fires; cross-`longTermGcCpuUsagePct` diagnosis for transient vs. sustained shifts. (2) G1HeapResize `longTermGcCpuUsagePct`: upgraded with sustained-trend semantics and cross-field diagnostic grid (short >> long = transient; short ≈ long = sustained). (3) ShenandoahCollectionDecision `generation`: upgraded from one-liners to per-value diagnostic — `"Young"` expected high frequency; `"Old"` cross-referenced to `triggerType`; `"Global"` is escalation/emergency; young-without-any-old diagnostic for stalled old-gen collection. (4) ShenandoahCollectionDecision `triggerType`: upgraded from "core diagnostic" to per-value tuning actions for all 10 trigger values — rate_average/accelerated/momentary, fragmentation, growth, expansion_failure, min_free, learning, pending, other. (5) ShenandoahCollectionDecision `fragmentationDensityPct`: upgraded with action path post-trigger — density recovery comparison across subsequent events; threshold for compaction working (> 70% recovery) vs. not keeping up (< 50% persistently → lower `ShenandoahOldGarbageThreshold`). (6) ShenandoahCollectionDecision `fragmentedFreeBytes`: upgraded from "bytes trapped" to severity gauge — `fragmentedFreeBytes / totalOldGenSize > 20%` = significant, > 40% = severe; not allocatable without compaction; multiple old-gen cycles required. (7) ShenandoahCollectionDecision `anticipatedGcDurationMs`: upgraded with cross-field formula (baseline = rate × duration), sustained growth diagnosis (workload getting harder), and stall-risk threshold (> 500ms with low `available`). (8) ZGCTenuringThreshold `tenuringThreshold`: upgraded with reason-specific per-value interpretation — `"Computed"` threshold 1–3 = aggressive (check old-gen frequency), 10+ = conservative, 4–7 = converged healthy; `"ZTenuringThreshold"` = static override, growing old-gen = override too high; `"Promote All"` = emergency mode, immediate old-gen spike expected.
- **Critique pass 54** (2026-08-18): Escalation-countdown and burst-detector precision. (1) ShenandoahReclaimProgress `badProgressCount`: upgraded from "value ≥ 2 = warning" to full escalation-countdown documentation — 0/1/2 state machine, `should_start_full_gc()` trigger condition, Full GC as correct-but-expensive escalation, action at count=1 (fix underlying cause), stall-risk cross-reference when `freePercent < 1%` and count is rising. (2) PSAdaptiveSizePolicy `promotedBytesLast`: upgraded from "spike relative to estimate" to burst-detector with 3× threshold, two root-cause paths (survivor overflow vs. humongous/direct), sustained-elevation threshold (3+ cycles = pattern shifted).
- **Critique pass 55** (2026-08-18): Systematic thin-field elimination — all fields now above 120-char tuning-use threshold. (1) NativeHeapTrim `beforeBytes`: upgraded from "pre-trim RSS; compare to afterBytes" to full purpose description — absolute RSS baseline, null-path guidance, coarse RSS trend use. (2) NativeHeapTrim `detailsAvailable`: completed truncated sentence — added `false`-persistent vs. `false`-transient interpretation, explicit "trim still ran" confirmation, redirect to OS-level RSS tools. (3) ShenandoahCollectionDecision `softMaxCapacity`: upgraded from formula tautology to `SoftMaxHeapSize < Xmx` container-environment pattern and action path. (4) ShenandoahCollectionDecision `baselineConsumptionBytes`: upgraded from "budget" to full formula cross-reference — `baselineConsumptionBytes / anticipatedGcDurationMs` = measured allocation rate in bytes/ms; trigger threshold confirmation diagnostic. (5) ShenandoahReclaimProgress 12-field table: all five row groups upgraded — core fields now document what each boolean means; `freeActual/freeNeeded` gets region-size formula and three-tier cross-reference; `usedFreed/usedNeededToFree` gets 1-region threshold explanation and zero-usedFreed diagnosis; internal/external frag rows get specific root causes and `ShenandoahUnloadClassesFrequency`/`ShenandoahGarbageThreshold` actions. (6) G1ConcurrentRefinementSweep `cardsNotClean`: upgraded from "ratio" to full effective-work-ratio analysis — high-clean diagnosis, declining-throughput diagnosis, cross-reference to `dirtiedCardRate`. (7) G1ConcurrentRefinementSweep `startTime`: upgraded from "timing" to inter-sweep interval analysis, join guidance with `jdk.GarbageCollection` for backlog trend per inter-pause window. (8) G1HeapResize `expand`: upgraded from "direction" to full per-value semantic — expand=true/false meaning, atLimit cross-ref, oscillation pattern diagnosis with `G1CPUUsageDeviationPercent` action. (9) G1HeapResize `resizeBytes`: upgraded from "0 on most pauses" to magnitude analysis — region count derivation, aggressive-jump risk on expansion, net committed-heap change tracking formula. (10) ZGCTenuringThreshold reason table: all five rows upgraded with specific consequence descriptions and differentiated actions. (11) ZGCTenuringThreshold `startTime`: added join guidance — gap to `jdk.ZYoungGarbageCollection.startTime` = selection phase duration. (12) ShenandoahTenuringThreshold pattern table: stable → baseline recording note added; min-clamped/max-clamped → consequence + risk-aware actions; sudden-drop → warm-up (50–100 cycles) vs. permanent-shift (50+ cycles) timeline. (13) G1CollectionSetCandidates `"Retained candidates exhausted"`: healthy state + Retained-vs-Marking accumulation cross-ref.
- **Critique pass 56** (2026-08-18): Second-tier precision pass — fields lacking concrete flags or thresholds upgraded. (1) ShenandoahCollectionDecision `marginOfError`: full calibration semantics — self-correction mechanism, interaction with rate triggers, growing-trend diagnosis for workload predictability. (2) ShenandoahCollectionDecision `liveAtPrevMarkBytes`: growth-rate formula with > 50% threshold, baseline-growing-over-events long-term trend diagnosis, action split between `ShenandoahOldGarbageThreshold` and `-Xmx`. (3) G1ConcurrentRefinementPolicy `dirtiedCardRate`: trend monitoring added, fundamental inequality framing, consequence when persistently high (thread-count ceiling reached). (4) PSAdaptiveSizePolicy `desiredEden`: `MaxNewSize` ceiling diagnosis, `edenSizingBranch` policy-trajectory cross-ref. (5) G1CollectionSetCandidates `candidateType`: upgraded to split-key analysis guidance — separate `selectedRegions/availableRegions` ratios and `stopReason` values per type for differentiated Marking-vs-Retained diagnosis.
- **Critique pass 62** (2026-08-19): "Why existing events" field-specificity pass (second sweep) — zero thin bullets remaining below 170 chars after this pass. (1) NativeHeapTrim: named `deltaBytes`, `trimDuration`, `trimCount` as the specific absent fields in `jdk.ResidentSetSize`. (2) GCOverheadLimitExceeded: named `_gc_overhead_counter`, `long_term_gc_time_ratio`, `free_space_percent` as absent from `jdk.GCCPUTime`. (3) ShenandoahMMU: PromotionInformation and HeapRegionStateChange bullets fully distinguished. (4) ShenandoahCollectionDecision: PromotionInformation bullet lists every absent field. (5) ShenandoahReclaimProgress: CollectionDecision merge-impossibility explained; "No existing event" terminator adds advance-warning consequence for `badProgressCount`. (6) ShenandoahTenuringThreshold: `compute_tenuring_threshold()` output fields named; TenuringDistribution input-vs-output distinction. (7) ZNMethodRegistration: ZNMethodTable structure separation from code cache documented. (8) G1ConcurrentRefinementSweep: 4 absent G1AdaptiveIHOP fields named; GarbageCollection bullet adds Update RS sub-phase. (9) G1CollectionSetCandidates: 5 absent GarbageCollection fields listed; conflicting-response consequence of missing stopReason. (10) G1HeapResize: 9 absent GCHeapSummary fields listed; GCConfiguration scale_with_heap() divergence noted. (11) ZDirectorRule: GarbageCollection bullet adds cause-string ambiguity; ZOldGarbageCollection outcome-only limitation. (12) PSAdaptiveSizePolicy: 9 absent GCHeapSummary fields listed; edenSizingBranch discriminator absence named. Vague-language fixes: NativeHeapTrim "not frequently enough" replaces "may be too long"; ShenandoahReclaimProgress deterministic conditional on freePercent; ZNMethodRegistration concrete rebuild-trigger investigation path.
- **Critique pass 63** (2026-08-19): Verdict upgrades and OQ resolution pass. ShenandoahCollectionDecision upgraded from "Redesign first" to "Propose with caveats" — redesign resolved: split into base event (single control thread site, `service_concurrent_normal_cycle()`) + `jdk.ShenandoahGCTrigger` follow-up (regulator thread); `decision` field dropped (always `"normal"` at sole emission point). ZGCTenuringThreshold upgraded from "Redesign first" to "Propose with caveats" — preferred approach: add `tenuringThresholdReason` field to existing `jdk.ZYoungGarbageCollection` (one-file diff in `zTracer.cpp`); standalone event is fallback design. All remaining "Recommendation:" and "Should X?" open questions converted to "Resolved:" with definitive decisions: GCOverheadLimitExceeded OQs 1,3,4; ShenandoahMMU OQ3; ShenandoahTenuringThreshold OQ1 (input fields excluded — log_debug); ZGCTenuringThreshold OQs 1,2; ShenandoahCollectionDecision OQs 1,2,3. Event Status Summary updated to reflect all resolved redesigns.
- **Critique pass 64** (2026-08-19): Precision and coverage pass. (1) ShenandoahCollectionDecision "The question it answers": expanded with the four-trigger-type diagnosis gap that post-GC heap summaries cannot fill; `available` headroom-at-trigger added. (2) ShenandoahCollectionDecision "What it is used for": added **Cross-event correlation** paragraph (join with ShenandoahMMU on gcId for trigger-vs-CPU-overhead, ShenandoahReclaimProgress for escalation chain, GarbageCollection for duration comparison). (3) ShenandoahReclaimProgress "The question it answers": expanded with `badProgressCount` 0→1→2 invisibility consequence and the one-step-earlier-warning value. (4) G1ConcurrentRefinementPolicy "The question it answers": expanded with `threadsWanted` as the invisible control-loop output and the progression-to-max-threads warning value. (5) ShenandoahCollectionDecision `cause=metadata_GC_threshold` field: "typically indicates" → "caused by" with specific action path (`MetaspaceSize`/`MaxMetaspaceSize`). (6) ZGCTenuringThreshold `tenuringThreshold` field for `reason="ZTenuringThreshold"`: "may be set too high" → "override is too high" with deterministic confirmation condition (growing old-gen occupancy) and removal action.
- **Critique pass 65** (2026-08-19): "Why existing events" terminator pass — 6 sections missing a standalone terminator bullet; all fixed. (1) NativeHeapTrim: "No other" → "No existing JFR event" with post-mortem diagnostic consequence. (2) ShenandoahMMU: "No Shenandoah-specific" → "No existing JFR event" with -Xlog fallback consequence. (3) ShenandoahCollectionDecision: added new terminator bullet naming absent heuristic fields and recording-level consequence. (4) ZNMethodRegistration: CodeCacheStatistics bullet extended with "Without `jdk.ZNMethodRegistration`" consequence clause for nmethod scan attribution. (5) G1ConcurrentRefinementPolicy: added new terminator bullet naming `adjust_threads_wanted()` invisibility and recording-level consequence. (6) G1HeapResize: added new terminator bullet listing all absent fields by name and the delta-derivability-vs-reasoning gap.
- **Critique pass 66** (2026-08-19): G1 event diagnostic depth pass — four targeted improvements. (1) G1ConcurrentRefinementSweep `cardsPending` bullet: replaced "increase threads" single-action with two-branch diagnosis on `threadsWanted` — (a) `threadsWanted < G1ConcRefinementThreads` → raise thread count ceiling; (b) `threadsWanted` at max → write rate exceeds capacity, only levers are application-level write reduction, `G1RSetUpdatingPauseTimePercent` increase, or `Xmx` increase. (2) G1ConcurrentRefinementSweep `cardRefineMs` bullet: replaced vague "balance against application threads" with two-branch saturation diagnosis keyed on `preSweepMs` ratio — (a) low `preSweepMs` with low `cardsNotClean / cardsScanned` → thread over-count (reduce `G1ConcRefinementThreads`); (b) `preSweepMs > cardRefineMs / 4` → bookkeeping overhead dominates (increase `G1HeapRegionSize`); added backlog-cross-check: both root causes only actionable if `cardsPending` is also growing. (3) G1HeapResize "What it is used for": replaced three question-form bullets with three diagnostic patterns — expand/shrink/atLimit branch table; `deviationCounter`-does-not-accumulate diagnosis; sigmoid oscillation pattern with `scaleFactorPct > 80%` + immediate `expand=true` followup detection and `G1CPUUsageShrinkThreshold` action. (4) OQ label fixes: G1ConcurrentRefinementSweep OQ3 "Recommendation:" → "Resolved:"; G1ConcurrentRefinementPolicy OQ3 "Recommendation:" → "Resolved:". README Event Status Summary updated to pass 66.
- **Critique pass 67** (2026-08-19): Thin-bullet and vague-language sweep across remaining events. (1) ShenandoahTenuringThreshold "Too-late promotion" bullet (155→320 chars): expanded with cohort-burst mechanism — objects accumulate in young gen until `maxTenuringAge` clamp causes a promotion surge visible as old-gen occupancy jump; added action (lower `maxTenuringAge` 1–2 to spread promotion). (2) ZGCTenuringThreshold "Baseline the computed threshold" bullet (132→570 chars): replaced vague "compare across deployments" with concrete workload examples (web service stable 3–5, batch 8–12), regression-signal framing (threshold drop from 6→2 after deployment = regression even without latency alarm), and cross-restart comparison guidance. (3) PSAdaptiveSizePolicy `promotedBytesEstimate` bullet (116→440 chars): replaced single-sentence with two-branch root-cause analysis — (a) `survivorOverflow=true` → survivor too small (`SurvivorRatio` reduction); (b) `survivorOverflow=false` + growing estimates → live set genuinely expanding at application level; in both cases added Full GC imminence criterion. (4) ZDirectorRule "Why existing events" `cause` string: "may be ZAllocationRate" → "is ZAllocationRate" (deterministic).
- **Critique pass 68** (2026-08-19): G1 verdict and question-section depth pass. All four G1 events had thin verdicts or question sections. (1) G1ConcurrentRefinementSweep verdict (70w→161w): added specific consequence framing — "documented tuning control whose observable data requires debug logging is an observability design gap"; named `Update RS` pause phase as the concrete symptom. (2) G1ConcurrentRefinementPolicy verdict (21w→187w): replaced "same caveat, consider joint PR" with a full standalone justification — unique argument for Policy (`threadsWanted` invisibility gap in a documented tuning control), and joint-PR rationale explained. (3) G1CollectionSetCandidates verdict (45w→215w) and question (90w→213w): verdict expanded to two-caveat numbered structure with `stopReason` enum implementation spelled out and the Oracle quote cited by name; question expanded with the "opposite-response" consequence — stopReason=time vs. stopReason=exhausted require opposite flag changes, making this a mandatory field for correct diagnosis. (4) G1HeapResize verdict (22w→211w) and question (90w→223w): verdict expanded to three-caveat numbered structure distinguishing debug-level, nullable fields, and emission gate concerns; question expanded with three specific unnamed-mechanism consequences (`deviationCounter` counter reset, `atLimit` blocking expansion, `scaleFactorPct` aggressive shrink).
- **Critique pass 70** (2026-08-19): Second G1 stability pass — remove remaining fragile internal references. (1) G1ConcurrentRefinementPolicy: stripped `g1Policy.cpp:803` line anchor from GC-pause emission site. (2) G1CollectionSetCandidates: removed `g1CollectionSet.cpp:446-513` and `:613` line anchors from `stopReason` field source; removed `:406` reference from OQ2; reformulated as "exhaustion log in g1CollectionSet.cpp" (file-level only). (3) G1HeapResize: removed `_recent_cpu_usage_deltas` private field name from sigmoid description; removed `decay_cpu_usage_tracking_data()` internal function name from deviation counter description; removed `scale_with_heap()` from `gcCpuUsageTargetPct` field tuning text and OQ4; removed `G1HeapSizingPolicy` class name and `_gc_cpu_usage_deviation_counter` from "Why existing events" prose; removed `g1CollectedHeap.cpp:988` line reference from OQ3 (verdict). All removals replaced with stable observable-behavior descriptions.
- **Critique pass 71** (2026-08-19): External references expansion — added JBS bug references to four events. (1) ZNMethodRegistration: added `#### External references` section (previously missing); added [JDK-8307058](https://bugs.openjdk.org/browse/JDK-8307058) (Generational ZGC ZNMethodTable architecture) and [JDK-8349652](https://bugs.openjdk.org/browse/JDK-8349652) (nmethod oop load barriers); moved orphaned quote block into the new section; added `#### Why existing events don't cover this` section. (2) G1ConcurrentRefinementSweep: added JEP 522 paragraph to External references — explains why the sweep state machine redesign motivates JFR observability; `cardsNoCrossRegion` field tied directly to write-churn JEP 522 optimized. (3) G1ConcurrentRefinementPolicy: added JEP 522 paragraph — `predictedPendingCards` and `dirtiedCardRate` fields measure the write-throughput characteristics JEP 522 targeted. (4) G1HeapResize: added [JDK-8238687](https://bugs.openjdk.org/browse/JDK-8238687)/[JDK-8247843](https://bugs.openjdk.org/browse/JDK-8247843) (GCTimeRatio redesign — changed default 12→24, introduced `resize_heap_after_young_collection()`; architectural basis for the `expand` boolean) and [JDK-8359348](https://bugs.openjdk.org/browse/JDK-8359348) (CPU measurement improvements — renamed to `long_term_gc_time_ratio()`; extended to include concurrent GC CPU; directly affects `gcCpuPct` semantics). (5) ShenandoahTenuringThreshold: replaced ZGC tuning guide quote (wrong cross-GC reference) with [JDK-8314599](https://bugs.openjdk.org/browse/JDK-8314599) ("GenShen: Couple adaptive tenuring and generation size budgeting") — the exact bug that introduced `compute_tenuring_threshold()` in `shenandoahAgeCensus.cpp`; field definitions tied directly to the three values the algorithm produces.
- **Critique pass 78** (2026-08-19): Schema precision and dead-weight field removal pass. Six issues fixed. (1) **Proposal #2 summary table `log_info(gc)` label clarified**: the `log_info(gc)` label is the decision-point log level in `satisfy_failed_allocation()`; the actual JFR emission site is `memAllocator.cpp` (no log call at all); summary table entry now distinguishes the two with "decision log; `memAllocator.cpp` (JFR emission site)" and names the nullable-field caveat. (2) **Proposal #17 `markCycleCompleted` field DROPPED**: field is always `false` at both emission sites — at the abort site because `has_aborted()` is the gate; at the overflow-reset site because `mark_cycle_completed` is not even in scope. A field that is always the same constant carries zero information — same reasoning that dropped `consecutiveViolations` from proposal #2. Added as Resolved OQ3. (3) **Proposal #18 `gcCause` field DROPPED**: `gc_cause()` is always `GCCause::_g1_compaction_pause` for all three escalation paths (set by `GCCauseSetter` at each call site). Zero-information constant field — same reasoning. `escalationPath` is the actual differentiator. Added as Resolved OQ4. (4) **Proposal #17 external references line number inconsistency FIXED**: references section cited line 1274 and 2744; emission-points section gives the authoritative numbers (~1282 and ~2790); aligned to the emission-points numbers with `~` prefix. (5) **Proposal #15 OQ1 inconsistency RESOLVED**: OQ1 recommended three separate events but implementation design section still described a single event with a `phase` discriminator — a direct internal contradiction. Implementation design section rewritten to match OQ1's recommendation (three events: `jdk.ShenandoahOldGarbageSurvey`, `jdk.ShenandoahOldEvacuationOutcome`, `jdk.ShenandoahOldThresholdAdjust`); OQ1 marked Resolved; `phase` field description clarified to note that it is a single-event fallback design. (6) **Proposal #15 summary table updated** to reflect three-event recommendation.
 (1) ZDirectorRule fields table: marked `timeUntilMinorOOM` and `minorFreeBytes` as DEFERRED (not in initial submission); removed the dangling 22-field name list that was a leftover fragment from the old design; fixed cross-event correlation paragraph and Why existing events bullets that referenced these fields as if present. (2) G1CollectionSetCandidates OQ3: changed from "keep two events" (contradicted the summary table) to "Marking path only for initial submission; Retained as follow-up" — now consistent with the table. (3) GCOverheadLimitExceeded fields table: removed `consecutiveViolations` row entirely (was listed as RECOMMENDED TO DROP — should not appear in the schema at all; OQ3 already resolved to drop it). (4) ShenandoahCollectionDecision impl difficulty: corrected from `~50 + ~40 lines` (double-counted trigger follow-up) to `~30 lines base event only`. (5) NativeHeapTrim verdict: replaced vague "a minor code change is required" with specific description of the RSS data gating fix (`logging_enabled ? &sc : nullptr` → unconditional). (6) ShenandoahReclaimProgress verdict: added explicit fallback — if full event rejected, propose `badProgressCount` as single field extension to `jdk.GarbageCollection`. (7) ZNMethodRegistration verdict: replaced vague "validate production concern" with three concrete evidence criteria that would justify filing upstream.
