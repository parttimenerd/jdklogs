# JFR Proposed Events — Research Index

Research documents supporting the proposed JFR events in `data/jfr-proposed-events.json`.

## Documents

### [Shenandoah Tenuring Threshold](shenandoah-tenuring-threshold.md)
How Shenandoah generational mode dynamically computes the tenuring threshold using mortality rate analysis. Covers the age census mechanism, the `compute_tenuring_threshold()` algorithm, `ShenandoahGenerationalCensusIgnoreOlderCohorts`, dark matter handling, and why this differs from G1/Parallel's static `MaxTenuringThreshold`. Supports `jdk.ShenandoahTenuringThreshold`.

### [G1 Concurrent Refinement](g1-concurrent-refinement.md)
How G1's dirty-card queue and concurrent refinement threads work. Covers the two distinct log sites (`print_refinement_stats` vs `adjust_threads_wanted`), card categories, the JDK 25 write barrier redesign (JEP 522), and why `jdk.G1ConcurrentRefinementSweep` + `jdk.G1ConcurrentRefinementPolicy` are needed.

### [ZGC Director Rules](zgc-director-rules.md)
How ZGC decides when to trigger Young and Old collections. Covers all rule types (Timer, AllocationRate dynamic/static, HighUsage, Warmup, Proactive), the early-exit evaluation model, the Major AllocationRate escalation path, and why the `jdk.ZDirectorRule` event is needed despite experimental `ZStatisticsCounter/Sampler` events.

### [Shenandoah Generational GC](shenandoah-generational.md)
How Shenandoah's generational mode (production-ready JDK 25, JEP 521) works. Covers collection types (young/old/global/mixed/degenerated/full), the regulator thread, remembered-set card statistics, MMU tracking, reclaim progress early-exit semantics, and the collection decision FSM. Supports `jdk.ShenandoahCardStatistics`, `jdk.ShenandoahMMU`, `jdk.ShenandoahReclaimProgress`, `jdk.ShenandoahCollectionDecision`.

### [Parallel GC Adaptive Sizing](parallel-gc-adaptive-sizing.md)
How `PSAdaptiveSizePolicy` implements the feedback loop for eden/survivor/old-gen sizing. Covers the throughput vs pause-time goal trade-off, gc-distance semantics, promoted bytes (averaged vs last), old-gen shrink, survivor overflow, and GCOverheadLimitExceeded in Parallel GC. Supports `jdk.PSAdaptiveSizePolicy` and `jdk.GCOverheadLimitExceeded`.

### [G1 Collection Set Candidates](g1-collection-set-candidates.md)
How G1 selects old-gen regions for mixed GC pauses. Covers the two separate selection paths (`select_candidates_from_marking()` and `select_candidates_from_retained()`), the groups concept, stop reasons, the optional evacuation threshold, and mixed-GC continuation decisions. Supports `jdk.G1CollectionSetCandidates`.

### [G1 Heap Resize Policy](g1-heap-resize.md)
How G1 resizes the committed heap between GC pauses using a CPU-usage deviation counter. Covers the `_gc_cpu_usage_deviation_counter` mechanism, upper/lower threshold derivation from `GCTimeRatio`, the shrink-path-only fields (`scaleFactorPct`, `freeRegions`, `regionsNeededForAlloc`), `atLimit` semantics, and the distinction from full-collection resizing. Supports `jdk.G1HeapResize`.

### [Native Heap Trimming](native-heap-trim.md)
Why native heap trimming matters for containerised JVMs (RSS bloat from glibc malloc arenas). Covers the `NativeHeapTrimmer` thread, trim operation mechanics, upstream PR history (JDK-8365306 closed), relationship to `jdk.ResidentSetSize`, and corrected `deltaBytes` sign semantics. Supports `jdk.NativeHeapTrim`.

### [ZGC Generational Mode](zgc-generational.md)
How ZGC selects a tenuring threshold each young collection. Covers the three selection paths (Promote All / ZTenuringThreshold config / Computed), the `compute_tenuring_threshold()` algorithm (life decay factor × log residency, scaled by allocation pressure), why `jdk.ZUncommit` already exists (EventZUncommit::commit() fires in update_statistics()), and why only the info-level result fields belong in the proposed event. Supports `jdk.ZGCTenuringThreshold`.

### [Event Consolidation & Emission-Point Analysis](event-consolidation.md)
Where each proposed event fires (exact function, call chain, cadence, thread), analysis of which events to merge/split, and field-level trimming recommendations. Key findings: `G1ConcurrentRefinementSweep`+`Policy` stay separate (different cadences/threads); `ShenandoahCollectionDecision`+`ReclaimProgress` stay separate (start vs end of cycle); `ZDirectorRule` should be redesigned as a per-tick summary (22 mostly-null fields → 6-field event); `ZNMethodRegistration.tableRebuilt` dropped (no source); `PSAdaptiveSizePolicy` loses 3 fields; `G1CollectionSetCandidates.continueMixed` dropped (different call site). `ShenandoahMMU.report()` path confirmed `log_debug`. Full emission-point table with calling function, cadence, and thread for all 16 events.


How Shenandoah generational mode decides when to start young and old collections. Covers the `log_trigger()` macro (resolves to `log_info(gc)` in production), the three adaptive heuristic trigger types (rate_average, rate_momentary, rate_accelerated), the three old heuristic triggers (expansion_failure, fragmentation, growth), the regulator FSM, and the `_margin_of_error_sd` field. Supports `jdk.ShenandoahCollectionDecision` trigger augmentation (critique pass 6).

## Event Status Summary (as of 2026-08-18, pass 39)

| Event | Priority | Status |
|---|---|---|
| `jdk.NativeHeapTrim` | high | Upstream PR (JDK-8365306) closed Dec 2025; narrow re-proposal recommended; `log_info(trimnative)` ✓; RSS collection gated on logging enabled — JFR impl must decouple |
| `jdk.GCOverheadLimitExceeded` | high | G1 support added JDK 26; no JFR event; fires at OOM throw only (`log_info(gc)` in `satisfy_failed_allocation()`); `GCOverheadLimitThreshold=5` is a `develop` flag (fixed in prod) |
| `jdk.ShenandoahMMU` | high | Zero coverage; `log_info(gc,ergo)` in `shenandoahMmuTracker.cpp:107,134` ✓; phase strings confirmed: "Concurrent Young GC", "Degenerated Young GC", etc. |
| `jdk.ShenandoahCollectionDecision` | high | Zero coverage; `decision` field confirmed from `gc_mode_name()` (line 765); trigger fields from `log_trigger()` (`log_info(gc)`); prepare_for_old_collections log_info at lines 567-572 confirmed |
| `jdk.G1ConcurrentRefinementSweep` | **medium** (↓ from high) | Zero coverage; **`log_debug(gc,refine)`** — below =info threshold; strong production case but debug-tier data |
| `jdk.G1ConcurrentRefinementPolicy` | **medium** (↓ from high) | Zero coverage; **`log_debug(gc,refine)`**; GC-pause path (g1Policy.cpp:1022) and periodic path (g1ConcurrentRefine.cpp:598) have different field shapes; threadsWanted not available on GC-pause path |
| `jdk.G1CollectionSetCandidates` | **medium** (↓ from high) | Zero coverage; **`log_debug(gc,ergo,cset)`** — ALL candidate selection logs are debug level |
| `jdk.ZDirectorRule` | **medium** (↓ from high) | Zero coverage; **`log_debug(gc,director)`**; start_gc() tries major first — if major fires, minor never evaluated; struct accumulation required for per-rule fields |
| `jdk.PSAdaptiveSizePolicy` | **medium** (↓ from high) | Zero coverage; **`log_debug(gc,ergo)`**; MaxGCPauseMillis default is unlimited (G1 overrides to 200ms); PSScavenge::invoke() at line 305 |
| `jdk.G1HeapResize` | medium (unchanged) | Zero coverage; **`log_debug(gc,ergo,heap)`**; deviationCounter field added; sigmoid scaling (steepness=6.0, inflection=1.0) confirmed |
| `jdk.ShenandoahReclaimProgress` | medium | Zero coverage; `log_info(gc,ergo)` in `shenandoahMetrics.cpp:47,58,69,81` ✓; free_space is hard gate (immediate false); ShenandoahCriticalFreeThreshold default = 1% |
| `jdk.ShenandoahTenuringThreshold` | medium | Zero coverage; `log_info(gc,age)` in `shenandoahAgeCensus.cpp:258` ✓ |
| `jdk.ZGCTenuringThreshold` | medium | **REDESIGN** — `jdk.ZYoungGarbageCollection` already has `tenuringThreshold` field (zTracer.cpp:104); proposed event is partially redundant; unique value is `reason` field (`"Promote All"`/`"ZTenuringThreshold"`/`"Computed"`); preferred upstream approach: add `reason` to existing event |
| `jdk.ZNMethodRegistration` | low | Weak production motivation; `log_info(gc,nmethod)` in `zStat.cpp:1621` ✓; jdk.NMethodSweep does not exist — removed incorrect reference |
| `jdk.StringDeduplicationStatistics` | low | **REDUNDANT** — `jdk.StringDeduplication` shipped in JDK 26 (PR #28015, merged Nov 2025) |
| `jdk.ShenandoahCardStatistics` | **blocked** | **BLOCKED** — data source is `#ifndef PRODUCT` guarded in `shenandoahCardStats.cpp`; requires moving stats collection to product build first |

## Critique History

- **Critique pass 1** (2026-08-17): Initial 5 events added (NativeHeapTrim, G1ConcurrentRefinement, ZDirectorRule, PSAdaptiveSizePolicy, ShenandoahCollectionDecision)
- **Critique pass 2** (2026-08-17): G1ConcurrentRefinement split into Sweep+Policy; PSAdaptiveSizePolicy averaged/last fields; GCOverheadLimitExceeded diagnostic fields; ZDirectorRule early-exit semantics; ShenandoahMMU priority elevated; ShenandoahReclaimProgress redesigned as flat event; ZNMethodRegistration tableRebuilt added; ShenandoahCardStatistics gated to cumulative-only
- **Critique pass 5** (2026-08-17): Local JDK source audit — function names corrected in G1CollectionSet and G1HeapSizingPolicy; ShenandoahCardStatistics demoted to 'blocked'; jdk.ShenandoahTenuringThreshold promoted (shenandoahAgeCensus.cpp:258 log_info confirmed)
- **Critique pass 6** (2026-08-17): ZGC generational source audit — jdk.ZGCTenuringThreshold promoted; ShenandoahCollectionDecision augmented with trigger fields; wrote zgc-generational.md and shenandoah-heuristics.md
- **Critique pass 7** (2026-08-17): Systematic log level audit — 5 events downgraded from high to medium; GCOverheadLimitExceeded redesigned to OOM throw point only; upstream research confirmed NativeHeapTrim PR closed, StringDeduplication shipped
- **Critique passes 8–9** (2026-08-17): Deep field sourcing — exact variable names, escalation thresholds, G1HeapSizingPolicy sigmoid, G1CollectionSet stopReason strings, PSAdaptiveSizePolicy three-source coordination, ShenandoahReclaimProgress CONSECUTIVE_BAD_DEGEN_PROGRESS_THRESHOLD=2
- **Critique pass 10** (2026-08-17): Links and quotes — all [file.cpp:N] references hyperlinked; exact log message quotes for all 14 events; PSAdaptiveSizePolicy call chain line numbers corrected
- **Critique passes 11–15** (2026-08-17): Source corrections — GCId::peek() semantics fixed (returns _next_id not last id); ShenandoahMMU degenerated phase strings corrected; G1ConcurrentRefinementPolicy two-path field asymmetry documented; ZDirectorRule major-preempts-minor semantics corrected; ShenandoahCollectionDecision decision field corrected to gc_mode_name() values; GCOverheadLimitThreshold=5 is develop flag (fixed in production); ShenandoahCriticalFreeThreshold default=1%; MaxGCPauseMillis default unlimited for Parallel GC; G1RefinementThresholdStep→G1RSetUpdatingPauseTimePercent; jdk.NMethodSweep removed (does not exist); G1ConcRefinementThreads (not G1ConcurrentRefinementThreads)
- **Critique pass 16** (2026-08-18): PSAdaptiveSizePolicy call chain correction — compute_desired_sizes() and compute_old_gen_shrink_bytes() confirmed in same ParallelScavengeHeap::resize_after_young_gc() scope; exact eden/survivor log quote added (psYoungGen.cpp:367)
- **Critique passes 17–20** (2026-08-18): Source code snippets added — is_good_progress() full source (shenandoahMetrics.cpp:38), compute_tenuring_threshold() loop (shenandoahAgeCensus.cpp:264), ZGC select/compute_tenuring_threshold() (zGeneration.cpp:704-816), sigmoid_function() (g1HeapSizingPolicy.cpp:97), stopReason from print_finish_message() (g1CollectionSet.cpp:399), start_gc() early-exit (zDirector.cpp:820), compute_desired_eden_size() 4-branch decision tree (psAdaptiveSizePolicy.cpp:86), execute_trim_and_log() RSS gate (trimNativeHeap.cpp:135), G1 GCOverheadLimitExceeded throw point (g1CollectedHeap.cpp:995-1111), update_utilization() GCU% formula (shenandoahMmuTracker.cpp:86), gc_mode_name() enum mapping (shenandoahGenerationalControlThread.cpp:765)
- **Critique pass 21** (2026-08-18): G1ConcurrentRefinementSweep cardsNoCrossRegion semantic fix — field was described as "same-region reference filter" but actual source (g1RemSet.hpp:122) confirms it counts write-churn cards (mutator changed all cross-region refs AFTER dirtying); tuning diagnosis updated
- **Critique pass 22** (2026-08-18): GCOverheadLimitExceeded field scope fix — long_term_gc_time_ratio and free_space_percent are locals inside update_gc_overhead_counter() and NOT in scope at the throw-point line 1109; must be re-read via accessor calls at emission site
- **Critique pass 23** (2026-08-18): PSAdaptiveSizePolicy field source corrections — promotedBytesEstimate fixed from _avg_promoted->padded_average() to AdaptiveSizePolicy::promoted_bytes_estimate() (adaptiveSizePolicy.hpp:203 = _promoted_bytes.davg() + _promoted_bytes.dsd()); minFreeBytes formula fixed from "10× rate × GC time" to max(padded_avg_promoted, promotion_rate × 600s) with 10-minute lookahead constant
- **Critique pass 24** (2026-08-18): ShenandoahReclaimProgress failedDimension redesign — field had 4 values but is_good_progress() only produces two failure paths: "free_space" (hard gate) and "all_secondary" (all three secondary checks failed); field table, evaluation examples, and tuning actions updated
- **Critique pass 25** (2026-08-18): ShenandoahTenuringThreshold algorithm description corrected from "weighted reciprocal" to accurate scan-down-and-find-oldest-high-mortality description; ShenandoahGenerationalCensusIgnoreOlderCohorts flag semantics documented; GCOverheadLimitExceeded Parallel GC gcTimePercent corrected to gc_time_percent() × 100 (fraction × 100, not 100 - mutator_pct); open question 4 clarified with G1 vs Parallel field-scoping distinction
- **Critique pass 30** (2026-08-18): External references added — Oracle GC tuning doc quotes, JEP links, and JBS/PR references added to each event's "External references" subsection. G1ConcurrentRefinementSweep/Policy: Oracle quotes on Pending Cards and G1RSetUpdatingPauseTimePercent + JEP 522. G1CollectionSetCandidates: Oracle quotes on gc+ergo+cset=debug, G1MixedGCCountTarget, G1MixedGCLiveThresholdPercent, G1HeapWastePercent. G1HeapResize: Oracle GCTimeRatio formula quote (1/(1+GCTimeRatio)=4%). PSAdaptiveSizePolicy: Oracle Parallel GC tuning quotes on throughput/pause goals, priority order, adaptive sizing. ShenandoahMMU/CollectionDecision/ReclaimProgress/TenuringThreshold: JEP 521 links and adaptive heuristics description. ZGCTenuringThreshold + ZDirectorRule: JEP 439 and Oracle ZGC tuning guide. NativeHeapTrim: PR #26756 design discussion. GCOverheadLimitExceeded: JDK-8212084 + PR #27950 description.
- **Critique pass 31** (2026-08-18): Diagnostic pattern tables added — ShenandoahTenuringThreshold "What it is used for" replaced with 5-row pattern table (stable/oscillating/minClamped/maxClamped/sudden-drop). ZGCTenuringThreshold "What it is used for" upgraded with per-`reason` diagnostic table (Computed stable/oscillating/low-threshold, ZTenuringThreshold static, Promote All emergency). Missing "Tuning use" columns added to NativeHeapTrim, GCOverheadLimitExceeded, ZNMethodRegistration, ShenandoahReclaimProgress, ShenandoahCollectionDecision field tables. ZNMethodRegistration "External references" section added (JEP 439 nmethod scanning cost quote). ShenandoahTenuringThreshold algorithm description corrected from "weighted reciprocal" to scan-down mortality analysis. ShenandoahMMU periodSeconds description fixed from tautology to CPU-time formula. G1HeapResize field descriptions augmented with GCTimeRatio=24 worked example. PSAdaptiveSizePolicy throughput/gcDistance field descriptions clarified with Parallel GC default GCTimeRatio=4 context. G1CollectionSetCandidates "What it is used for" upgraded with stopReason → tuning action mapping table.
- **Critique pass 32** (2026-08-18): JFR metadata audit against `src/hotspot/share/jfr/metadata/metadata.xml`. Key findings: (1) `jdk.ZYoungGarbageCollection` already has a `tenuringThreshold` field — ZGCTenuringThreshold verdict changed from "Propose" to "Redesign first"; preferred upstream approach is adding `reason` field to existing event. (2) `jdk.GCCPUTime` explicitly does not cover Shenandoah (metadata description + no GCTraceCPUTime in Shenandoah paths) — added to ShenandoahMMU "Why existing events" as explicit gap evidence. (3) `jdk.ShenandoahEvacuationInformation` and `jdk.ShenandoahPromotionInformation` exist — added to "Why existing events" sections for ShenandoahCollectionDecision, ShenandoahReclaimProgress, ShenandoahTenuringThreshold to show no overlap. (4) `jdk.ZStatisticsCounter`/`ZStatisticsSampler` are experimental (confirmed) — added to ZDirectorRule. (5) `jdk.ZAllocationStall` added as complementary reference in ZDirectorRule (failure-case vs. prevention-side). (6) `jdk.OutOfMemoryError` does NOT exist in JFR — corrected from "(hypothetical)" to factual statement; added GCCPUTime to GCOverheadLimitExceeded "Why existing events" section.
- **Critique pass 33** (2026-08-18): "The question it answers" sharpening pass. (1) GCOverheadLimitExceeded: removed stale `jdk.OutOfMemoryError (if it exists)` phrase; reframed around "no JFR event fires at the GC decision point" and impossibility of post-hoc analysis on OOM. (2) G1CollectionSetCandidates: corrected "start or stop mixed GCs" to "selection stopping within an already-running mixed GC"; added concrete stopReason framing so readers understand what question cannot be answered today. (3) G1HeapResize: clarified that delta is computable from G1HeapSummary diffs but policy drivers (deviation counter, thresholds, scale factor) are not; removed misleading "partial redundancy" implication. (4) G1ConcurrentRefinementSweep: reframed from throughput description to backlog→pause-time impact ("residual backlog must be drained during the next GC pause under G1RSetUpdatingPauseTimePercent budget"). (5) ZGCTenuringThreshold fields table: `tenuringThreshold` field annotated as redundant with existing `jdk.ZYoungGarbageCollection` field; `reason` field annotated as the unique value of the proposal.
- **Critique pass 34** (2026-08-18): Precision and concrete-example pass. (1) ZNMethodRegistration "The question it answers": reframed from "how many nmethods" to scanning-overhead impact and staleness detection. (2) PSAdaptiveSizePolicy "The question it answers": added the oscillating-policy failure mode ("eden grows, pauses spike, eden shrinks, throughput drops, repeat") as the uniquely undetectable case without this event. (3) G1ConcurrentRefinementPolicy "What it is used for": added concrete numeric worked example (`dirtiedCardRate=500 cards/ms`, `predictedRefineRate=200 cards/ms × 2 threads = 400 cards/ms < demand`) showing how the imbalance is invisible below debug-log level. (4) ZDirectorRule fields table: `triggeredMinorRule` nullable semantics clarified — null means "evaluated/nothing triggered" OR "not evaluated because major fired"; disambiguated via `triggeredMajorRule != null`. Open question 4 (which documented this ambiguity) removed as it is now in the fields table.
- **Critique pass 35** (2026-08-18): Field precision and open-question resolution pass. (1) NativeHeapTrim `deltaBytes`: sign semantics (negative = returned to OS, positive = allocation outpaced reclaim) moved into field table Source column; redundant prose paragraph removed. (2) PSAdaptiveSizePolicy `desiredSurvivor` field: expanded from tautology to concrete survivor/eden comparison use; `survivorOverflow` co-diagnosis note added. (3) PSAdaptiveSizePolicy diagnosis patterns: oscillating-policy pattern added to the bulleted list (alternating `throughputEdenIncrease` and `minorPauseMs > pauseGoalMs`). (4) PSAdaptiveSizePolicy `throughputEdenIncrease`: annotated as candidate for replacement by `edenSizingBranch` string; open question 3 rewritten as a concrete 5-value recommendation. (5) ZDirectorRule "The question it answers": corrected "ZGC was never evaluated" (wrong — director always evaluates); reframed around silence on no-trigger ticks. (6) ZDirectorRule diagnostic patterns: major-preempts-minor null semantics surfaced in the `triggeredMajorRule="_z_allocation_rate"` pattern. (7) ShenandoahMMU "The question it answers": added `jdk.G1MMU` field names (`gcTime` vs `pauseTarget`) to make the structural difference concrete. (8) G1CollectionSetCandidates `availableRegions` field: explained what determines the candidate count (`G1MixedGCLiveThresholdPercent` liveness filter) and what low `availableRegions` implies. (9) ShenandoahCollectionDecision `cause` field: added cause-value semantics; `available` field: added fill-fraction formula and `ShenandoahMinFreeThreshold` tuning note. (10) G1HeapResize open question 4 (`gcCpuUsageTargetPct`): resolved to "keep" with justification that `scale_with_heap()` makes the effective target non-derivable from `GCTimeRatio` alone.
- **Critique pass 36** (2026-08-18): "Why existing events" field-specificity audit. Five sections upgraded from generic "no event covers X" to field-name-level claims: (1) GCOverheadLimitExceeded: `jdk.GCCPUTime` expanded with specific missing fields (`_gc_overhead_counter`, `long_term_gc_time_ratio`, `GCHeapFreeLimit`); `jdk.GCHeapSummary` expanded to name the outcome-only fields it carries. (2) ShenandoahTenuringThreshold: added `jdk.GarbageCollection` explanation (cause string carries no mortality-rate data); `jdk.TenuringDistribution` expanded to explain why the age distribution input ≠ the threshold output. (3) G1ConcurrentRefinementSweep: `jdk.EvacuationInformation` and `jdk.G1AdaptiveIHOP` expanded with their actual fields; added final bullet listing the specific fields only the new event provides. (4) G1CollectionSetCandidates: `jdk.EvacuationInformation` expanded with its actual fields (`cSetRegions`, `cSetUsedBefore`, etc.); `jdk.G1HeapSummary` and `jdk.GarbageCollection` added; final bullet lists absent fields by name. (5) G1HeapResize: `jdk.G1AdaptiveIHOP` expanded with its actual fields; `jdk.G1HeapSummary` expanded; `jdk.GCConfiguration` added. (6) PSAdaptiveSizePolicy: `jdk.PSHeapSummary` expanded with its actual fields; `jdk.GCConfiguration` added; final bullet lists absent per-cycle decision fields by name.
- **Critique pass 37** (2026-08-18): "Why existing events" precision pass (continued). (1) G1ConcurrentRefinementPolicy: replaced generic "same analysis" placeholder with specific missing fields — `threadsWanted` and `pendingCardsTarget` named as the unique outputs of `adjust_threads_wanted()` not observable in any existing event. (2) ZNMethodRegistration: `jdk.CodeCacheStatistics` expanded with its actual fields (`entryCount`, `methodCount`, `adaptorCount`, `unallocatedCapacity`) and explicitly distinguished from `ZNMethodTable` — a separate data structure; `_nunregistered` stale slots and per-GC scan costs named as absent. (3) NativeHeapTrim: `jdk.ResidentSetSize` expanded with its actual fields (`size`, `peak`), its `period="everyChunk"` cadence, and a precise statement of what it cannot do — no `deltaBytes` field, no trim attribution, no single-operation isolation. (4) NativeHeapTrim `jdk.ResidentSetSize` stale version qualifier "(JDK 27 master)" removed from "The question it answers"; "(JDK 27)" removed from "External references" — event already present in local metadata.xml. (5) ShenandoahTenuringThreshold: removed duplicate `jdk.GarbageCollection` bullet (identical content appeared twice).
- **Critique pass 38** (2026-08-18): Open-question resolution pass. (1) NativeHeapTrim OQ2 resolved: `deltaBytes` as signed `long` is the right design — `os::size_change_t` has unsigned `before`/`after`; `deltaBytes = (long)afterBytes - (long)beforeBytes` is computable at emit time. OQ3 resolved: `_num_trims_performed` confirmed cumulative from JVM init at `trimNativeHeap.cpp:169`. (2) ShenandoahMMU: `gcuPercent`/`muPercent` source formula expanded — `_active_processors` is fixed at JVM startup via `os::initial_active_processor_count()`; container CPU quota changes post-startup are not reflected. `periodSeconds` corrected to "wall-clock since previous call to `update_utilization()`", not "GC phase duration". OQ2 resolved: `gcId` from `record_*` callers is reliable; `record_old_marking_increment()` intentionally excluded (rolls up into next cycle). (3) ZGCTenuringThreshold OQ3 resolved: `gcId` reliable at `select_tenuring_threshold()` time — `GCIdMark` set in `ZDriverScopeMinor` before `collect()` is called. (4) G1HeapResize OQ3 resolved: event should gate on `resizeBytes != 0` — `bool& expand` reference is only assigned inside expand/shrink branches; stale when neither fires. `expand` field description updated with this note. (5) PSAdaptiveSizePolicy OQ4 resolved: both `gcDistanceSec` and `gcDistanceSecLast` are needed — `compute_desired_eden_size()` uses `.last()` as the branching input; `.davg()` shows the trend.
- **Critique pass 39** (2026-08-18): Open-question resolution pass (continued). (1) G1ConcurrentRefinementPolicy OQ2 resolved: keep `predictedPendingCards` — confirmed as `num_cards + incoming_rate × time_until_gc` at `g1ConcurrentRefineThreadsNeeded.cpp:64`; it is the projected backlog at the next GC, which is what the thread-count algorithm targets. (2) ShenandoahTenuringThreshold OQ2 resolved: `gcId` is the right correlation key — `GCIdMark` set at `shenandoahGenerationalControlThread.cpp:248` before `prepare_regions_and_collection_set()` runs. (3) ShenandoahMMU OQ1 resolved: drop `isPeriodicSample` and the periodic path from the initial proposal — `report()` is `log_debug(gc)` only; `isPeriodicSample` field removed from event definition.
